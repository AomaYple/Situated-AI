# ─────────────────────────────────────────────────────────────
# 本仓库的忽略规则
#
# 原则：交付内容（文档 + 可复现脚本 + 研究资料）全部入库；
#       只排除「机器相关配置」与「可由脚本重新生成的中间产物」。
# ─────────────────────────────────────────────────────────────

# 【机器相关】本机 TLS 配置（含证书包，换机器即失效）
/.dsh/


# ── tools/ 的中间产物 ──────────────────────────────────────
# 说明：tools/ 的 11 个脚本（含 README）已入库，以下三个子目录未入库。

# 提取出的大型 JSON（约 3.3 MB），由 dump_*.ps1 从游戏文件重新生成
/tools/out/

# 由 make_frags.ps1 从 tools/out/*.json 渲染出的 Markdown 片段
/tools/frag/

# 分块撰写的正文草稿。注意：这不是「可重新生成的产物」，
# 而是 assemble_doc.ps1 的输入；其内容已 100% 拼入 docs/ 下的成稿，
# 因此排除不会丢失任何信息（已验证 14/14 段全部命中成稿）。
/tools/docparts/


# ── 备注 ───────────────────────────────────────────────────
# .research/ 已按要求纳入版本控制，不再忽略。
# 其中 official-docs/ 是游戏自带官方 .md 的逐字镜像（Paradox 版权内容），
# 公开分发前请自行评估。
