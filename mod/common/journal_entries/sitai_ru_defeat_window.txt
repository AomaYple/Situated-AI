# ⚠️ 本文件由 `v3 modgen` 生成（tools/pdx/modgen.py）—— 改这里没用，改数据源 mod/data/*.toml。

# 改革窗口 JE（俄罗斯 · 战败求存）：判据驱动 —— 只有压力够大才开。
# 这个 JE 的作用**只有三条**（2026-09-22 实测复核，别读多）：
#   ① **玩家可见性**：G3 的三行解释挂在它身上（P11）；
#   ② **世界状态判据**：原版脚本可以用 `has_journal_entry = <本 JE 名>` 读它；
#   ③ **递牌**：`immediate` 里给该国换一张**原版**策略牌（见下面的 signals 依据）。
# ⚠️ **它喂不到原版 AI 牌**：原版 212 处 `has_journal_entry` 全部硬编码**具名 JE**
# （`ai_strategies/03_political_strategies.txt:137` 读的是 je_metternich 这一串），
# **没有「读任意 JE」的通用谓词** —— 我们自己的 JE 一张牌也喂不到。
# 意图位移的真正通道是**递牌**（`set_strategy`）与**法律承诺**（law commitment），
# 原因与实测见 `docs/design/backlog.md` B52 / B53–B60。
# 递牌依据：递牌的依据（P10：每个数字/决定都要能指到原文）。**修的是哪个具体毛病**：原版重抽是累积掷骰（CHANGE_STRATEGY_THRESHOLD = 100、每周 20% 概率 +1 点 → 期望 ≈9.6 年一次，00_ai.txt:38-39），而战败窗口只开几年 ⇒ 等重抽等于不递牌。**手段为什么是 B 级而不是 A 级**（F5）：世界状态改得了 AI 的输入，改不了它**已经挂着的那张牌** —— 而牌才是「动不动手」的闸门。**为什么递原版自己的牌、不建新牌**：每国同时只有一张政治牌在用（阶段 2 实测 1.05%/国·月），自建牌是抢槽；递原版已有的牌不新增竞争，只把落点拨到那条路线。**为什么进步牌是对的落点**：`law_tenant_farmers` 的 +1000 分支要求「非反动 **且**（非保守 **或** 有运动赞成且支持 ≥10%）」（01_land_reform.txt:216-231），而土地改革运动对这两条法分别是 `neutral` / `disapprove`（03_ig_ideologies_movement.txt:720-721）⇒ 走运动那一支是假的，只有「非反动且非保守」那一支成立。递进步牌同时满足两条，且它是三张牌里唯一 `change_law_chance = 10` 的。

je_sitai_ru_reform_window = {
	icon = "gfx/interface/icons/event_icons/event_portrait.dds"
	group = je_group_internal_affairs

	is_shown_when_inactive = {
		c:RUS ?= this
		has_variable = sitai_ru_defeat_memory
	}

	possible = {
		has_variable = sitai_ru_defeat_memory
		legitimacy <= 75
	}

	complete = {
		legitimacy >= 75
	}

	weight = 100

	immediate = {
		set_strategy = ai_strategy_progressive_agenda
	}

	on_complete = {
		set_strategy = ai_strategy_reactionary_agenda
	}
}