# ⚠️ 本文件由 `v3 modgen` 生成（tools/pdx/modgen.py）—— 改这里没用，改数据源 mod/data/*.toml。

# 奥地利 · 革命潮 · 冲击记账（A 级：真实世界状态 —— 变量 + 修正，不占任何槽位）。
# 调用方：A/B 实验（B 组显式施加冲击）。本档案刻意**不绑 on_action**：「什么才算这次冲击」的判定留给实验给结论，
# 由谁写、写在哪，见本档案 why 与数据源（每个数字都带依据，P10）。
# 变量 sitai_au_revolution_memory；压力修正 sitai_au_revolution_pressure。

sitai_au_revolution_shock = {
	set_variable = {
		name = sitai_au_revolution_memory
		value = 1
		days = 3650
	}
	add_modifier = {
		name = sitai_au_revolution_pressure
		years = 10
	}
}

# 改革侧输入（A 级：同样是真实世界状态）：**实验手段**（与 [pressure] 同一定位）：它是「改革派牌读得到的那两个输入」的直接修正，用来把 A/B 阶梯的第二处理段做成可施加、可关掉的一处。走 A 级而不是递牌的理由（F5）：原版 `ai_strategy_progressive_agenda` 的权重是**读**这两个 IG 的，改世界状态就等于改它的输入，不占任何槽位。⚠️ 正式的因果链应当是「革命危机 → 旧秩序失能 → 工业化/识字率的真实位移 → IG 势力自然变化」，而不是直接改政治力量；那条已进 backlog（B35）。**为什么「革命潮」也需要这一处**：革命潮开的是**改革窗口**，而窗口里真正被推着走的仍然是原版那两张读 IG 势力的议程牌 —— 只压合法性、不抬改革侧输入，等于只开门不给路。　【沿用·口径】本表的数值与依据**原样沿用** `ru_defeat.toml` 的实测档位（阶段 3 的 A/B 读数），**未针对本条处境独立校准**。
# 为什么单独一个效果：实验的 B 段只施加冲击、B2 段才追加这一处 ——
# 两处合并在一个效果里就分不出是哪一处起了作用（执行文档：两层分开报）。
# 修正 sitai_au_reform_inputs；效果名 sitai_au_reform_input（不绑 on_action，由实验显式调用）。

sitai_au_reform_input = {
	add_modifier = {
		name = sitai_au_reform_inputs
		years = 10
	}
}