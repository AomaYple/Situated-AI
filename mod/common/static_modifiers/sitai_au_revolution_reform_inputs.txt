# ⚠️ 本文件由 `v3 modgen` 生成（tools/pdx/modgen.py）—— 改这里没用，改数据源 mod/data/*.toml。

# 改革侧输入（A 级）：**实验手段**（与 [pressure] 同一定位）：它是「改革派牌读得到的那两个输入」的直接修正，用来把 A/B 阶梯的第二处理段做成可施加、可关掉的一处。走 A 级而不是递牌的理由（F5）：原版 `ai_strategy_progressive_agenda` 的权重是**读**这两个 IG 的，改世界状态就等于改它的输入，不占任何槽位。⚠️ 正式的因果链应当是「革命危机 → 旧秩序失能 → 工业化/识字率的真实位移 → IG 势力自然变化」，而不是直接改政治力量；那条已进 backlog（B35）。**为什么「革命潮」也需要这一处**：革命潮开的是**改革窗口**，而窗口里真正被推着走的仍然是原版那两张读 IG 势力的议程牌 —— 只压合法性、不抬改革侧输入，等于只开门不给路。　【沿用·口径】本表的数值与依据**原样沿用** `ru_defeat.toml` 的实测档位（阶段 3 的 A/B 读数），**未针对本条处境独立校准**。
# 每条字段的依据见 sitai_au_revolution.md（由 why_report 生成）。
# ⚠️ 这一份**不是**冲击本身：它是实验的第二处理段（B2）才挂的东西 ——
# 写进 [pressure] 会让「只加冲击」的那一段消失。

sitai_au_reform_inputs = {
	icon = gfx/interface/icons/timed_modifier_icons/modifier_lightbulb_positive.dds
	interest_group_ig_industrialists_pol_str_mult = 0.5
	interest_group_ig_intelligentsia_pol_str_mult = 0.5
}