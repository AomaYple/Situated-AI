"""性能剖析子包。

``prof/`` 是**可重复运行**、产出可对比报告的正式剖析工具（历史上曾并存一组
一次性的排障脚本 ``tools/diag/``，那批脚本已随工具链收敛而删除）。

全部基于成熟库，不自己写计时框架：

* ``cProfile`` / ``pstats``   标准库，精确到函数级的累计耗时与调用次数
* ``pyinstrument``            调用树，直接看出时间花在哪条路径上
* ``typer`` / ``rich``        命令行与表格渲染
"""
