# ⚠️ 本文件由 `v3 h1-probe` 生成（tools/pdx/h1_probe.py）—— 改这里没用，改生成器。
# 只覆盖两个键，不复制原版 00_ai.txt（KB 05 §1.7：按「块 + 参数」覆盖；
# Kuromi 的 kai_ai.txt 也只写了一个 NAI 块）。
#
# 原版值：CHANGE_STRATEGY_THRESHOLD = 100、INCREASE_WEEKLY_CHANCE = 20（1 = 1%）。
# 原版语义 → 攒够 100 个「变更点」才重抽一次；每周只有 20% 概率 +1 点，
# 所以单靠周通道要 ≈9.6 年才重抽一次（这正是政治槽实测 0.37%/月 的原因）。
# 本变体把门槛压到 1、周概率拉满 → 每周都重抽：大样本剂量反应 + 验证
# 「mod 能不能接管重抽节奏」。
NAI = {
	CHANGE_STRATEGY_THRESHOLD = 1
	CHANGE_STRATEGY_INCREASE_WEEKLY_CHANCE = 100
}
