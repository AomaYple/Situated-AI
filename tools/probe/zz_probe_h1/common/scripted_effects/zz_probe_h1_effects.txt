# ⚠️ 本文件由 `v3 h1-probe` 生成（tools/pdx/h1_probe.py）—— 改这里没用，改生成器。
# ⚠️ **本文件里只能有裸效果列表**（scripted_effects 的语法）：写成 on_action 那种
#    `effect = { … }` 包装会被引擎读成「调一个叫 effect 的效果」→
#    `Unknown effect effect`，整个定义作废，而 on_action 那头只会报
#    `No on_action scripted with tag … cannot link`（实测踩过：分组一次都没跑，
#    DOSE 行全是 CTRL）。钩子/包装在 `zz_probe_h1_on_actions.txt` 里。
#
# ① `zz_probe_h1_assign`：随机分组，`random_list` 25/25/25/25 → 四组。
#    组间**唯一**差异是探针牌的权重。幂等（`sitai_probe_dose_assigned` 标记）：
#    两个 on_game_started 钩子都会调它，重复调用是常态。
# ② `zz_probe_h1_boot`：开局写一行 RUN 标记 —— 分析器靠它切分「哪次启动的数据」
#    （日志轮转会把多次启动的行混在同一个目录里）。
#
# H3（`add_ai_strategy` 语法）已结案并从探针里移除：add_ai_strategy 不是脚本效果（两种语法都是 Unknown effect）。
zz_probe_h1_assign = {
	every_country = {
		if = {
			limit = { NOT = { has_variable = sitai_probe_dose_assigned } }
			set_variable = { name = sitai_probe_dose_assigned value = 1 }
			random_list = {
				25 = { set_variable = { name = sitai_probe_dose_low value = 1 } }
				25 = { set_variable = { name = sitai_probe_dose_mid value = 1 } }
				25 = { set_variable = { name = sitai_probe_dose_high value = 1 } }
				25 = { }
			}
		}
	}
}

zz_probe_h1_boot = {
	zz_probe_h1_assign = yes
	debug_log = "ZZPROBE H1;RUN;storm"
}
