# ⚠️ 本文件由 `v3 ab-probe` 生成（tools/pdx/ab_probe.py）—— 改这里没用，改生成器。
# 阶段 3 的引擎侧判定套件（H2：真实冲击 → AI 行为层差分）。
#
# 结构见游戏目录的 tools/scripted_tests/scripted_tests.md：last_date = 这一套跑多久；
# tests.<名字> = { success / fail / run_count }，两个触发器**每天**检查，success 先判，
# 到 last_date 都没命中则判「跳过」（所以 fail 的日期必须早于 last_date）。
#
# 两条判据与 `pdx.ab` 同源：主指标 = 改革窗口；次指标 = 法律是否换过。
# 日期口径：阶梯第 37 月（≈1839.1）才追加改革侧输入，给 2 年余量 →
# fail 定在开局后第 61 个月（1841.1.1），last_date 再多半年。

last_date = "1841.6.1"

tests = {
	reform_window_opens = {
		acceptable_fail_rate = 0.0
		run_count = 1
		# 成功 = 改革窗口开过（行为层①）。
		success = {
			c:RUS ?= { has_journal_entry = je_sitai_ru_reform_window }
		}
		# 失败 = 到日期仍未开窗（不是「跳过」：跳过会让整件事悄悄过去）。
		fail = {
			game_date > "1841.1.1"
		}
	}

	law_changed = {
		acceptable_fail_rate = 0.0
		run_count = 1
		# 成功 = 农奴制已经不是现行法律（行为层②：真的换了法，不只是开了个窗）。
		success = {
			c:RUS ?= { NOT = { has_law = law_type:law_serfdom } }
		}
		fail = {
			game_date > "1841.1.1"
		}
	}
}
