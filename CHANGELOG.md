# 发布说明（CHANGELOG）

> Victoria 3 mod「**Situated AI**」的发布说明。**玩家读的是这一份**。
>
> **格式是机器可查的**（`v3 release` 会核，见 `docs/design/exec/阶段7-长期维护.md`）：
>
> * 版本标题写成 `## [<x.y.z>] - <日期>`；**最新那一节**必须与 mod 元数据的 `version` 一致；
> * 档案条目一律写成列表项，id 用反引号包住、后跟一个破折号：
>   ``- `ru_defeat` — 俄罗斯 · 战败求存：一句话说明它修的是什么毛病``
>   ——`mod/data` 里**每一份**档案都必须在这里被写过（新加档案忘了写 = 玩家看不到它），
>   而这里提到的每个 id 也必须真的存在（删了档案却留着说明 = 说明在骗人）；
> * 缺哪些条目可以跑 `v3 release --template` 打印骨架（只打印，那句话得人来说）。
>
> 「三个槽位 / 不替换原版 / 只看官方」这些主张见 `docs/design/01-大方向.md`；
> 每条数字的依据见数据源 `mod/data/*.toml` 的 `why`。

## [0.1.0] - 2026-09-23

第一版：**处境档案库**（9 份）+ 生成它的那套 Python 工具链。

这一版的主张只有一句话：**原版 AI 照常运行，我们只补它缺的能力** ——
不替换、不并行第二套 AI、不关任何原版子系统；让 AI 在不同**处境**下改变**意图的落点**。
每份档案做的是同一件事：**把原版没有记的那笔账记成真实世界状态**（记忆变量 + 真实压力 + 改革窗口），
然后让**原版自己**那张权重公式转向。

### 处境档案（9 份）

- `ru_defeat` — 俄罗斯 · 战败求存：战败之后 AI 照旧走原来的路 —— 贵族在朝、合法性不坠、财政不受罚，
  于是「战败 → 改革」这条因果在原版里根本不存在。本档案把战败记成账。
- `tr_defeat` — 奥斯曼 · 战败求存：同上，主角换成那个被叫作「欧洲病夫」的帝国。
- `au_revolution` — 奥地利 · 革命潮：「帝国是从内部裂开的」这件事在原版里没有账 ——
  合法性照旧、激进派照旧被压着，于是「革命危机 → 改革」这条因果不存在。
- `cn_intervention` — 大清 · 列强干涉：原版记了「被打穿」那一刻及其结果（赔款、通商），
  没记**因果**：士绅与旧官僚照旧在朝，玩家看到的是一个挨了打却不会改的大清。
- `eg_debt` — 埃及 · 债务危机：原版只在**宣布破产那一刻**记一次，且记的是结果；
  债主是谁、钱被谁管着、为什么从此只能按别人的账本过日子 —— 一个字都没有。
- `pe_great_game` — 波斯 · 大博弈：原版把「大博弈」记在**争的两强那一侧**，
  被夹击的那一方在账本上不存在。
- `sp_empire_remnant` — 西班牙 · 帝国残骸与内战：原版把这场王位内战**记到战争结束那一刻为止** ——
  帝国已经丢光、本土打完一场内战之后，合法性靠什么站起来、教会与军队各自落到哪里，原版不记。
- `bv_alignment` — 巴伐利亚 · 倒向谁：原版替**集团**与**两强**记了账，
  **没替被选的那一方记账** —— 一个中等邦国倒向某一方之后要付什么代价，世界上一个字都没有。
- `brz_market_loss` — 巴西 · 失去市场：前八份的处境都有个「谁」（打你的、夹你的、讨债的、
  要你选边的），巴西没有那个谁 —— 它的收入长在一条别人说了算的商路上。本档案把这条路记成账。

### 可见性与难度

- 每份档案都带**三行解释**（当前目标 / 最大压力与阻力 / 上次改主意的原因），
  接在 journal entry 的说明里直接上屏（P11：可观测）。
- **难度三档**（`game_rules`）：处境压力对谁都成立，难度调的是**玩家肩上的那一份**。

### 工具链（`tools/pdx/`，Python）

mod 是**生成**的，不是手写的：数据源 `mod/data/*.toml` →`v3 modgen`→ `mod/` 下的产物
（脚本 / 本地化 / 文档），手改产物会被闸门 ④ 报红。

- **五道闸门**：键与原版零交集 · 引用完整性 · 稀释预算 · 往返净度 · 生成可复现；
- **234 条断言**（`v3 verify`）+ **171 张生成表**（`v3 tables`）+ **850 条 `文件:行号` 引用**
  （`v3 citations`）—— 文档里的每个数字都被机器核过，每条依据都指得到原版的那一行；
- **实机 A/B**：`python -m pdx.game_auto run --wait-tests N` 一条命令跑完
  起游戏 → 观察 → 速度 → 切后台 → 等引擎自己的判定 → 读产物（不通过退 1）。

### 已知的边界（如实写在这里，不藏）

- **意图层与立法层逐档案、逐局不同**：三份跑过整局的档案里，窗口都开了、
  世界状态都被改了，但**目标法都没换**（一条推了投票权那一组、两条一条法没推）。
  这不是"没效果"，而是**效果落在哪一层**还没被完全解开（归因见 `docs/design/backlog.md` B53–B60）。
- **两条判据表达不出来**：巴伐利亚那份记的是"选边的**代价**"，记不了"选了**哪一边**"；
  巴西那份记的是"出口部门的**后果**"，记不了"市场通道"本身。两处都写明了让步，
  没有用特例糊过去。
- **难度三档的界面**还没被人眼确认过（观察者局进不到开新局的规则界面）。
