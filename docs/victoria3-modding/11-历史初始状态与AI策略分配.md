# 11 · 历史初始状态与 AI 策略分配

> 本文覆盖 `common\history\` 系统，其中 `history\ai\` 是**给 AI 设定开局策略**的地方 ——
> 这是 `03-AI系统.md` 未覆盖的关键一环：定义策略只是「有哪些策略可选」，这里决定「谁一开始用哪个」。

> **版本提示（精确清单）**：本机游戏是 **1.14.4 (Ice Tea)**（版本指纹 `env.caligula_rev` = `d369a59ea65e68d4cca455ddd558c497958927f9`、`env.caligula_branch` = `release/1.14.4`，两条断言本次 `v3 verify` 已核验）。原先那句笼统的「统计采集于 1.14.2」按类拆开：
> - **已是 1.14.4 —— 表格已重算**：本篇 **1 张**表（`doc11 history 子目录`，即 §1 的 22 个子目录 / 1,153 个文件）由 `v3 tables` 生成；本次 `v3 tables` 核对「全部 171 张生成表都与文档一致」。
> - **已是 1.14.4 —— 断言已核验**：本篇 **2 条** `hist.*` 断言（`hist.subdirs_doc11`、`hist.files_doc11`）随 `v3 verify` 现场重扫，本次 **234/234 通过**（含本篇）。
> - **已复测的「未确认 / 未整理」类结论**（1.14.3 实测值，逐条见 §7）：`set_strategy` 全树 **124 处 / 13 文件**（§3.2 的「89 次调用 / 27 个 `c:TAG` / 1 个 `every_country`」实测不变）；`00_secret_goals.txt` 从「仅读了开头」补成**完整效果清单**（**6 类效果 / 573 处键**）；③ 本机 23 个 mod **0 个**用 `set_strategy`（本机快照）。
> - **未复测（明确列出）**：§4 那张 27 国表的逐国 `type` 字段对齐（1.14.2 逐条核对过，本轮未重跑）、§5.2 `00_behavior_variables.txt` 的 4 国变量与 `ai_strategies` 条件的联动（属 `03-AI系统.md` 范围）。
> 已经过自动核验的数量断言见 `v3 verify`（其断言表已更新到 1.14.4）；文档与断言表的一致性由 `tools/tests/test_docs_consistency.py` 持续看守。

## 1. `common\history\` 全貌

**【实测】** 22<!--claim:hist.subdirs_doc11--> 个子目录，共 **1,153<!--claim:hist.files_doc11--> 个文件**，根目录下无松散文件。

| 子目录 | 文件数 | 作用 |
|---|---|---|
| `countries` | **444** | 各国初始状态（法律、科技、国库、政府等） |
| `population` | **373** | 各州人口构成 |
| `characters` | **262** | 历史人物（统治者、将领、鼓动者…） |
| `pops` | 17 | POP 相关 |
| `buildings` | 16 | 各州初始建筑 |
| `military_formations` | 10 | 初始军队编制 |
| `diplomacy` | 6 | 初始外交关系 |
| `diplomatic_plays` | 5 | 进行中的外交博弈 |
| `governments` | 5 | 初始政府组成 |
| **`ai`** | **3** | **AI 初始策略与行为** ← 本文重点 |
| `conscription` · `cultures` · `global` · `government_setup` · `lobbies` · `military_deployments` · `political_movements` · `power_blocs` · `production_methods` · `states` · `trade` · `treaties` | 各 1 | 单项初始化 |

> 命名规则：文件名通常是 `国家TAG - 国家名.txt`（如 `swe - sweden.txt`），
> 但 `ai\` 下用的是功能名（`00_strategy.txt` 等）。

## 2. `history\ai\` —— 三个文件

**【实测】**

| 文件 | 字节 | 作用 |
|---|---|---|
| `00_strategy.txt` | 5,669 | **开局分配 AI 策略**（`set_strategy`） |
| `00_secret_goals.txt` | 15,022 | 初始化国家的秘密目标 |
| `00_behavior_variables.txt` | 343 | 设置 AI 行为变量 |

## 3. 核心机制：`set_strategy`

**【实测】** `00_strategy.txt`（176 行）的结构：

```pdx
AI = {
	 # Default AI subject starting strategy depends on LD
	every_country = {
		limit = { is_subject = yes }
		if = {
			limit = { liberty_desire >= 80 }
			set_strategy = ai_strategy_subject_break_free
		}
		else_if = {
			limit = { liberty_desire >= 60 }
			set_strategy = ai_strategy_subject_strengthen_self
		}
		else_if = {
			limit = { liberty_desire <= 20 }
			set_strategy = ai_strategy_subject_overlord_compliance
		}
		else = {
			set_strategy = ai_strategy_subject_maintain_autonomy
		}
	}

	c:SWE ?= {
		set_strategy = ai_strategy_resource_expansion
		set_strategy = ai_strategy_maintain_power_balance
		set_strategy = ai_strategy_conservative_agenda
	}
	# ... 其余国家
}
```

### 3.1 语法要点

| 语法 | 含义 |
|---|---|
| `AI = { ... }` | 文件级包装块（**所有 `history\ai\` 文件都是这个顶层键**） |
| `c:<TAG> ?= { ... }` | 针对特定国家。**`?=` 是安全赋值运算符** —— 国家不存在时不报错 |
| `every_country = { limit = {...} ... }` | 遍历所有国家 |
| `set_strategy = <ai_strategy_键名>` | **分配一个策略** |
| `set_variable = { name = X days = N }` | 设置带时限的变量 |
| `if` / `else_if` / `else` / `random_list` | 块内可用完整脚本逻辑 |

> `set_strategy` 的取值必须是 `common\ai_strategies\` 里定义过的键（如 `ai_strategy_industrial_expansion`）。

### 3.2 关键结构规律：每个国家 3 个策略

**【实测】** 全文 **89 次 `set_strategy` 调用，27 个 `c:TAG ?=` 块，1 个 `every_country` 块**。

绝大多数国家块里**恰好 3 条 `set_strategy`**，分别对应三种 `type`：

```text
c:SWE ?= {
    set_strategy = ai_strategy_resource_expansion      ← administrative
    set_strategy = ai_strategy_maintain_power_balance  ← diplomatic
    set_strategy = ai_strategy_conservative_agenda     ← political
}
```

也就是说：**多数国家在每个策略类别上最多持有一个策略**，正好呼应 `03-AI系统.md` §2.3 中 `type` 字段的三种取值。**但有例外**：`FIN` 的三条里有两个 `diplomatic`、零个 `political`（见 §4 的表）。

### 3.3 特殊案例：JAP 用 `random_list` 做分支

**【实测】** `c:JAP ?= { ... }`（第 127 行起）内含 `random_list`，按天保危机状态随机选择不同的策略组合：

```pdx
c:JAP ?= {
	if = {
		limit = { has_journal_entry = je_tenpo_crisis }
		random_list = {
			50 = {
				set_variable = { name = ai_tenpo_reform_agenda days = 4380 }
				set_strategy = ai_strategy_progressive_agenda
				set_strategy = ai_strategy_placate_population
				set_strategy = ai_strategy_armed_isolationism
			}
			50 = {
				# 另一套组合
			}
		}
	}
	else = {
		# ⚠️ 这个 else 分支同样有 3 条 set_strategy，不能漏
		set_strategy = ai_strategy_placate_population
		set_strategy = ai_strategy_armed_isolationism
		set_strategy = ai_strategy_conservative_agenda
	}
}
```

> 这是「9 次调用出现在一个 JAP 块里」的原因 —— **2 个随机分支 + 1 个 `else` 分支** × 3 个策略。
> ⚠️ 照抄时**不要只抄 `random_list`**：漏掉 `else` 就意味着天保危机未发生时该国一条策略都没有。

## 4. 完整 TAG → 策略分配表

**【实测】** 27 个国家，按文件顺序（`?=` 表示该国不存在时静默跳过）：

| TAG | 行政类 (administrative) | 外交类 (diplomatic) | 政治类 (political) |
|---|---|---|---|
| `SWE` | `resource_expansion` | `maintain_power_balance` | `conservative_agenda` |
| `SAR` | `industrial_expansion` | `maintain_power_balance` | `progressive_agenda` |
| `PAP` | `placate_population` | `maintain_power_balance` | `conservative_agenda` |
| `TUS` | `resource_expansion` | `maintain_power_balance` | `progressive_agenda` |
| `BRZ` | `plantation_economy` | `maintain_power_balance` | `progressive_agenda` |
| `ARG` | `industrial_expansion` | `territorial_expansion` | `progressive_agenda` |
| `SIC` | `agricultural_expansion` | `maintain_power_balance` | `reactionary_agenda` |
| `FIN` | `resource_expansion` | `maintain_power_balance` | `subject_strengthen_self`<sup>⚠️</sup> |
| `AUS` | `agricultural_expansion` | `maintain_power_balance` | `conservative_agenda` |
| `HUN` | `agricultural_expansion` | `subject_strengthen_self` | `progressive_agenda` |
| `CRO` | `agricultural_expansion` | `subject_maintain_autonomy` | `progressive_agenda` |
| `FRA` | `industrial_expansion` | `maintain_power_balance` | `progressive_agenda` |
| `PRU` | `industrial_expansion` | `maintain_power_balance` | `conservative_agenda` |
| `RUS` | `agricultural_expansion` | `colonial_expansion` | `reactionary_agenda` |
| `TUR` | `resource_expansion` | `maintain_power_balance` | `tanzimat_reforms` |
| `GBR` | `industrial_expansion` | `economic_imperialism` | `progressive_agenda` |
| `BIC` | `plantation_economy` | `subject_maintain_autonomy` | `conservative_agenda` |
| `DEI` | `plantation_economy` | `subject_maintain_autonomy` | `conservative_agenda` |
| `USA` | `industrial_expansion` | `economic_imperialism` | `conservative_agenda` |
| `EGY` | `plantation_economy` | `territorial_expansion` | `conservative_agenda` |
| `CHI` | `placate_population` | `armed_isolationism` | `maintain_mandate_of_heaven` |
| `JAP` | 随机分支（见 §3.3） | 随机分支 | 随机分支 |
| `CAM` | — | `subject_break_free` | — |
| `SER` | `agricultural_expansion` | `subject_break_free` | `conservative_agenda` |
| `MOL` | `agricultural_expansion` | `subject_break_free` | `progressive_agenda` |
| `WAL` | `agricultural_expansion` | `subject_break_free` | `conservative_agenda` |
| `MON` | `montenegro_admin` | `armed_isolationism` | `conservative_agenda` |

> 表中策略名均省略了 `ai_strategy_` 前缀。
> 注意 `CAM` 只分配了 1 个策略（不满足「3 个」的规律，属特例）。
>
> <sup>⚠️</sup> `FIN` 的第三条 `subject_strengthen_self` 在 `ai_strategies` 里声明的
> `type` 是 **`diplomatic`**，不是 `political` —— 也就是说 `FIN` 有两个外交类、
> 零个政治类。全表 27 国里**只有这一处**与「三类各一」的规律不符
> （其余 26 国逐条核对过 `type` 字段，全部吻合）。

### 4.1 属国的全局默认规则

`every_country` 块对所有属国按**自由渴望（liberty_desire）**分配：

| 条件 | 分配的策略 |
|---|---|
| `liberty_desire >= 80` | `subject_break_free` |
| `liberty_desire >= 60` | `subject_strengthen_self` |
| `liberty_desire <= 20` | `subject_overlord_compliance` |
| 其他（21–59） | `subject_maintain_autonomy` |

> 这是**唯一**按条件动态分配的全局逻辑；其余国家都是硬编码。

## 5. 另外两个文件

### 5.1 `00_secret_goals.txt`（15 KB）

**【实测】** 用于初始化国家的秘密目标。开头的逻辑结构：

```pdx
AI = {
	every_country = {
		limit = {
			any_diplomatically_relevant_country = {
				OR = {
					is_in_same_power_bloc = prev
					has_friendly_diplomatic_pact = { TARGET = prev }
				}
			}
		}
		save_scope_as = first_country
		every_country = {
			limit = { ... }
			effect_set_non_hostile_mutual_secret_goal_power_difference = yes
		}
	}
}
```

初始化逻辑：**同一权力集团或有友好条约的国家之间，互相设置非敌对秘密目标**。
文件内注释说明了意图（大意）：历史上的敌国不应一开始就表现出和解倾向，除非在历史脚本中明确写出。

涉及的关键效果（【实测】文件内出现）：

- `effect_set_non_hostile_mutual_secret_goal_power_difference`
- 依赖的触发器/条件：`is_in_same_power_bloc`、`has_friendly_diplomatic_pact`、`has_diplomatic_pact`、`any_diplomatically_relevant_country`
- 作用域工具：`save_scope_as`、`prev`

> 秘密目标的分值字段是 AI 策略里的 `secret_goal_scores`（见 `03-AI系统.md` §2.4）。

### 5.2 `00_behavior_variables.txt`（343 B）

**【实测】** 全文只有一个块，用于设置历史行为变量：

```pdx
AI = {
	# Set historical tendencies for Ottomans vs Egypt
	c:GBR ?= {
		set_variable = veiled_protectorate_support_ottomans
	}
	c:AUS ?= { set_variable = veiled_protectorate_support_ottomans }
	c:RUS ?= { set_variable = veiled_protectorate_support_ottomans }
	c:PRU ?= { set_variable = veiled_protectorate_support_ottomans }
}
```

即：给英、奥、俄、普四国打上 `veiled_protectorate_support_ottomans` 变量标记，
用于在第 1836 年「埃及—奥斯曼危机」中表达历史倾向。

> 变量名可在 `ai_strategies` 的策略条件里用 `has_variable = <名>` 读取
> —— 这提供了一条**从历史脚本向 AI 决策传参**的通路。

## 6. 在 mod 中如何改

### 6.1 场景 A：让某国开局走不同策略

追加自定义文件到 `common\history\ai\`，用同样的 `AI = { c:TAG ?= { set_strategy = ... } }` 结构。

> ⚠️ **注意覆盖语义**：同一 TAG 若被多处 `set_strategy`，行为取决于引擎如何合并（**【未确认】**）。
> 更安全的做法是**直接替换原版 `00_strategy.txt`**（与 `Kuromi's AI` 替换 `00_default_strategy.txt` 同理）。

### 6.2 场景 B：新增一个策略并让它被使用

两步：

1. 在 `common\ai_strategies\<你的文件>.txt` 定义 `ai_strategy_<你的名字> = { ... }`
2. 在 `common\history\ai\` 里用 `set_strategy = ai_strategy_<你的名字>` 分配

> 注意：新策略若想让 AI **自动随机选中**，还需要 `possible` 触发器与 `weight` 权重
> （见 `03-AI系统.md` §2.3）；而 `history` 里的 `set_strategy` 是**强制指定**，绕过随机分配。

### 6.3 场景 C：用 `every_country` 写通用规则

比逐个 TAG 硬编码更优雅，且能自动适配 mod 新增的国家：

```pdx
AI = {
	every_country = {
		limit = { is_subject = yes }
		if = { limit = { liberty_desire >= 80 } set_strategy = ai_strategy_subject_break_free }
	}
}
```

## 7. 未确认项

> 证据口径与四条实测配方见 `04-脚本系统.md` §13.1（A 脚本化测试 / B 调试日志 / C 最小 MOD / D 双 MOD 冲突）。

| # | 未确认内容 | 本地证据（可复算） | 状态 |
|---|---|---|---|
| 1 | 【未确认】多处对同一 TAG `set_strategy` 的合并规则 | ① `v3 evidence set_strategy` → 原版键 **124 处 / 13 文件**，按目录：`common/history/ai` **89**、`events` **24**、`events/japan_events` **3**、`events/iberia_events` **2**、`events/balkans_events` **2**、`common/journal_entries` **2**、`events/brazil` **1**、`common/effect_localization` **1** —— 即同一 TAG 确实会被**两处写**（JAP：`AI/c:JAP` 6 处 + `events/japan_events` 3 处 + `meiji.13` / `ep2_meiji.1` 各 3 处）；③ 本机 23 个 mod **0 个**用 `set_strategy`（本机快照，`v3 evidence --mods set_strategy`）→ 「谁最终生效」**无实例可判** | 【未确认】待实测 → 配方 D |
| 2 | 【未确认】`set_strategy` 是否有次数上限 | ① 逐块计数（`common/history/ai/00_strategy.txt`）：**28 个顶层子块、合计 89 处**，单块最多 **9 处**（`c:JAP`，§3.3 的 3 分支 × 3）、其余 25 国各 3 处、`c:CAM` 1 处、`every_country` 4 处；② 官方 md **0 处**；④ exe 有字面量（`set_strategy` 与 `set_secret_goal` 相邻）—— 但**没有任何上限声明或超限实例** | 本地无证据（观测上限 9 处/国）→ 配方 A |
| 3 | 策略在游戏中途能否用 `set_strategy` 更换 | **已答：「history 专用」不成立**。① 124 处里 **35 处不在 history**（`events/` 32 + `common/journal_entries` 2 + `common/effect_localization` 1），父键链都是**运行时触发**：`warlord_china_events.100/immediate/if/create_country/on_created/random_list/1`（12 处）、`meiji.13/immediate/hidden_effect`（3 处）、`colonial_administration_events.1/option/scope:newly_formed_colonial_nation_scope`（4 处）、`german_unification.2/option/hidden_effect/…`（`set_secret_goal` 侧同型）；④ exe 有字面量。⚠️ 能换 ≠ 能随意换：更换后旧策略是否被顶掉仍属第 1 条 | 已答 |
| 4 | `00_secret_goals.txt` 的完整效果清单 | **已答（本轮全文机械提取）**：全文 **72 个不同键 / 573 处**，其中**效果只有 6 类** —— `set_secret_goal` **132**、`set_mutual_secret_goal` **19**、`effect_set_non_hostile_mutual_secret_goal_power_difference` **7**、`effect_set_limited_hostile_mutual_secret_goal` **1**、`effect_set_limited_non_hostile_mutual_secret_goal` **1**、`effect_set_non_hostile_mutual_secret_goal` **1**；作用域工具 `save_scope_as` **8**、遍历 `every_country` **5**；其余键是触发器（`country_has_primary_culture` 6、`country_rank` 4、`is_in_same_power_bloc` 2…）与子块头（`country` 151、`secret_goal` 151）。全树侧：`set_secret_goal` **177 处 / 18 文件**、`set_mutual_secret_goal` **33 处 / 7 文件**（复算：`v3 evidence set_secret_goal set_mutual_secret_goal`） | 已答 |
