# exec · 接续说明（**冷启动先读这一页**）

> **用途**：新会话 / 换了机器 / 隔了很久再回来时，**先读这一页**，再按 §2 的顺序展开。
> **本页只写三件事**：现在在哪、下一步做什么、怎么跑。别的都不重复 —— 一律给链接。
>
> **维护规则**：每次会话收尾时**只更新 §1「当前坐标」**，其余各节是稳定的。
> 本页不写"最后一次提交的哈希"（它无法包含自己）—— 一律用 `git log --oneline -1` 现看。

---

## 0. 这个仓库是什么（30 秒）

Victoria 3 **1.14.3** 的 mod「**Situated AI**」+ 一套把 mod 当软件来造的 **Python 工具链**。

* **想要什么**：见 `../01-大方向.md`（**冻结文件，不许编辑**）。一句话是「原版 + 我们 > 原版」：
  不替换、不并行第二套 AI、不关任何原版子系统，而是让 AI 在不同**处境**下改变**意图的落点**。
* **怎么造**：mod 是**生成**的，不是手写的 —— 数据源 `mod/data/*.toml` →`v3 modgen --write`→
  `mod/` 下的产物（脚本 / 本地化 / 文档）。**不许手写产物**（P3）。
* **怎么证明**：门禁（§4）+ 实机 A/B（`自动化范式.md`）+ 每条数字都带 `why`（引官方行号 / 探针日志 / 知识库章节）。

---

## 1. 当前坐标（**每次收尾更新这一节**）

| 项 | 值 |
|---|---|
| 阶段 | **阶段 5（档案扩张）· 批次 1–3 已落地**；**批次 3 的结果文档还没写** |
| 档案 | **8 份**：`ru_defeat` · `tr_defeat` · `au_revolution` · `cn_intervention` · `eg_debt` · `pe_great_game` · `sp_empire_remnant` · `bv_alignment` |
| 产物 | **60 个**，与数据源逐字节一致（`v3 modgen --check` 实测） |
| 引用 | **770 条** `文件:行号` 引用全部指得到唯一一行（`v3 citations` 实测） |
| 门禁 | `v3 verify` **234 / 234** · `v3 modguard` **5 / 5** · `v3 tables` **171 张** · `v3 ai-surface --check` · `v3 cov`（下限 86%） |
| 用例 | **1434 条**（1426 通过 / 8 按条件跳过） |
| 目标状态 | 会话目标已按用户要求 **暂停** ⇒ **用户不说"继续"就不要自动往下做** |
| 工作树 | 收尾提交后应当是**干净**的；若 `git status --short` 不空，先看 §6 第 1 行 |

**本批次的位置细节与未完成项**：`阶段5-批次3-收尾交接.md`（那一页是"这一批"的，本页是"整个项目"的）。

---

## 2. 冷启动读入顺序

| # | 读什么 | 为什么读它 |
|---|---|---|
| 1 | `../01-大方向.md` | 目标 · 原则 · **冻结清单** · 路线图 · 验收 G1–G4 · 实现期纪律。**冻结，只读** |
| 2 | `../01a-依据与参考.md` | 输入因子 · **手段阶梯 A–E** · 引擎实测 · 22 国示例初判表。选手段的依据 |
| 3 | **本页 §1 + §3** | 现在在哪、下一步做什么 |
| 4 | `阶段5-批次3-收尾交接.md` | 本批次的位置与未完成项 |
| 5 | `收口清单.md` | 每批 / 每阶段收口必须 A→E 走一遍（含清零残留的命令） |
| 6 | `自动化范式.md` | 实机流程与控制台手法（**要开游戏就必须先读它**） |
| 7 | `../backlog.md` | 悬而未决的问题 + 推荐值 + **触发条件**（B 编号） |
| 8 | `阶段7-长期维护.md` | 门禁全表 · 时间预算 · CI 矩阵 · 还欠的四件事 |

---

## 3. 下次开工的三件事（按顺序，别跳）

1. **整跑一遍全量套件**（上一轮修完夹具后没整跑过，这是唯一没闭环的验证）：
   ```powershell
   .venv\Scripts\python.exe -m pytest tools/tests -q -n auto     # ≈3.5 分钟，期望 1426 passed / 8 skipped
   ```
2. **写 `阶段5-批次3-结果.md`**（西班牙 + 巴伐利亚）。必须如实写进去的一条：
   巴伐利亚的**抽象缺口** —— 「**倒向谁**」这个选择在现有能力里**表达不出来**，
   能表达的只有"倒向"这件事的**代价**。**缺口不许写成成绩**（`../01-大方向.md` §5 失败处理：
   档案加不进去 ⇒ 说明抽象不对，改抽象、不加特例）。
3. **补记 `eg_debt` 的 A/B 结果**（做了但还没落文档）：改革窗口**开** ✅ · 冲击**触发** ✅ ·
   改革派**没有**入阁 ❌ · clannish `intelligentsia b18` vs `landowners b25` · `ENACT none` **27/27**。
   同时**顺手复核**新加的 `ZZPROBE AB;TARGET;<档案 id>` 自臂行 —— 它只在观察者局的自臂路径上产出，
   **还没在实跑的 A/B 里被看到过一次**（只验过代码路径）。

**再往后**（阶段 5 之后）：阶段 5 剩余模式「失去市场」→ 阶段 6 的难度规则**界面确认** + 人的 G3/G4 试玩
→ 阶段 7 的 `v3 citations` 进 CI（**B77**，当前 CI 的唯一实质缺口）+ 发布流程工具 → 阶段 4 ④ 的标准压力剧本。

---

## 4. 怎么跑（确切命令）

```powershell
# 环境（已建好；换机器才需要）
.venv\Scripts\python.exe -m pip install -r requirements.lock

# 生成：数据源 → 产物
.venv\Scripts\v3.exe modgen                      # 只编译，不写盘
.venv\Scripts\v3.exe modgen --write              # 写盘（生成物一律由它产出）
.venv\Scripts\v3.exe modgen --check              # 盘上产物与数据源逐字节一致？→ CI 门禁
.venv\Scripts\v3.exe modgen --why                # 把每个数字的依据展开

# 门禁（完整一轮 ≈6 分钟；分项耗时见 阶段7-长期维护.md §三）
.venv\Scripts\v3.exe verify                      # 234 条断言：文档里的数字还对不对
.venv\Scripts\v3.exe tables --check              # 171 张生成表还与游戏一致吗
.venv\Scripts\v3.exe modguard                    # 五道闸门
.venv\Scripts\v3.exe ai-surface --check          # 与原版 ai_strategies / defines 的对照面
.venv\Scripts\v3.exe citations                   # 770 条 文件:行号 引用还指得到吗
.venv\Scripts\ruff.exe check . ; .venv\Scripts\ruff.exe format --check . ; .venv\Scripts\mypy.exe
.venv\Scripts\python.exe -m pytest tools/tests -q -n auto
.venv\Scripts\v3.exe cov                         # 覆盖率门禁
```

**五条会咬人的口径**：

1. **并行是默认**（`addopts` 带 `-n auto --dist loadscope`）；**基准必须 `-n0`** ——
   `pytest-benchmark` 在 xdist 开启时会**静默禁用自己**，不带 `-n0` 时基准根本不会跑。
2. **`v3 cov` 固定 `-n 4`**（不是 `auto`）：16 核下 `-n auto` + 覆盖率会 376 秒且偶发假红，
   `-n 4` 是 185 秒全绿（`../backlog.md` **B28**）。
3. **`v3 analyze` 之后必须 `v3 check-outputs`**：前者刷新 `tools/out/*` 与 `tools/reports/*`，
   后者核"落盘产物有没有把断言写对"。改了现场数字却忘了刷新产物 ⇒ `test_check_outputs` 红。
4. **跑过仪器 / 探针之后 `test_engine_crosscheck` 会红**：那是"引擎日志是探针会话"这条按条件跳过
   在起作用（正常），但 `test_cli.py::test_crosscheck_与引擎日志一致` 会因为 mod 集合不同而红 ——
   跑一次 `.venv\Scripts\python.exe -m pdx.game_auto run` 即可恢复。
5. **离线通道证明的是"与入库快照一致"，不是"与现在的游戏一致"**：后者永远是本机门禁
   （`verify` / `tables` 不带 `--offline`）。**exit 2 不是"闸门不过"，是"这台机器上跑不了"**，
   CI 里把 2 当通过是错的（`../../tools/README.md` 的环境矩阵）。

**实机**（一次 ≈4–5 分钟）：

```powershell
.venv\Scripts\python.exe -m pdx.game_auto run    # 起游戏 → 前台 → 加载期不碰窗口 → 点「观察」→ 5 档速度 → 空格 → 切回后台
.venv\Scripts\python.exe tools\probe\stage3_rerun.py --months 12 [--archive <id>] [--no-probe] [--fresh-logs]
.venv\Scripts\python.exe tools\probe\perf_compare.py 12 --repeat 3     # 性能 A/B（≈1 小时）
```

**开游戏前必须先看 `收口清单.md` §B.0** —— 那一段是"清零残留"的一条命令版，
用来确认：游戏进程 0 个、用户 `content_load.json` 是**原样**的、仓库里没有垃圾文件。

---

## 5. 协作口径（用户明确要求过的，**别再问一遍**）

| 口径 | 出处 |
|---|---|
| **逐文件 `git add`，禁止 `git add -A`** | `收口清单.md:118` |
| 提交信息用**中文**、不加 `feat:` / `fix:` 等英文前缀；写清做了什么、关键数字、为什么这么设计 | `README.md` 开发约定 · `收口清单.md:119` |
| 推送算完成：`git log --oneline origin/main..HEAD` **为空**才算推上去了 | 本仓库一贯口径 |
| **测试默认并行**（`-n auto`），只有基准用 `-n0` | `pyproject.toml` `addopts` |
| 每批 / 每阶段收尾**必须**走 `收口清单.md` A→E，任何一项红 ⇒ **阶段不算结束** | `收口清单.md` 开头 |
| 加牌 / 加档案后**必须**查占槽率（和平期 ≈ 0），并在结果文档记账 | `../01-大方向.md` §5 |
| 每写一个数字（权重 / 阈值）都要 `why`，引到官方行号 / 探针日志 / 知识库章节 | `../01-大方向.md` §5 |
| **用户口径**：不要中途停下来问、一口气做完；用户没让启动游戏就不要启动；等状态一律条件等待（`wait_until`）、**不要直接 sleep** | 会话内多轮明确要求 |
| 所有文件 **UTF-8 无 BOM + LF** | 站内纪律 |
| 暂停就是暂停：用户说停之后**不要自动续跑** | 会话内明确要求 |

---

## 6. 踩过的雷（读一遍能省几小时）

| 症状 | 真相 / 纪律 |
|---|---|
| 提交了但工作树还是脏的 | 先 `git status --short` **逐项**看；`.gitignore` 里 `tools/out/` 是产物、`tools/reports/` 是**入库**的人读报告 |
| 控制台敲了命令没反应 | 反引号开控制台后，**必须按两次回车**才提交（自动补全吃掉了第一次）。判据走画面 ROI —— `../../tools/README.md` 那张 `game_auto.py` 行 · backlog **B66** |
| 脚本崩了，用户的 mod 配置却已经被改 | **崩在 `finally` 之前**：`finally` 根本不会执行。任何"改用户环境"的脚本，**第一次改动之前不许有会抛的操作**（挪日志→再改配置）。backlog **B79** |
| 改了 A，B 却 `NameError` | "从 A 到 B 整段替换"的手术把**夹在中间的函数**一起切掉了；静态检查全绿（探针脚本没有用例守着）。**整段替换前先把那一段打出来确认**。backlog **B78** |
| `.yml` 被写坏、`_reason` 的值被拆成几行 | 生成器里拼文本时换行必须是**字面量** `\n\n`（两个字符），不是真换行。backlog **B75** |
| 产物改了，`test_golden` 红 | 金标准指纹要**重新生成**（`test_artifact_digests.yml`），它不是"自动跟随"的 |
| 一条判据在另一份档案上**立刻为真** | 任何"跨档案通用"的判据都要问这句话。硬编码 `law_serfdom` 就是这么暴露的。backlog **B80** |
| `modguard` 的合成夹具又红了 | 夹具"按需列出原版有什么"，每加一份档案都要补词（tag / 图标 / 分组 / 触发器 / 修正字段）。**闸门没错，夹具的设计有代价**。backlog **B82** |
| 文档里"本机现场"的数字过期 | 现场类断言（订阅了几个 mod、共几个文件）会随**用户自己**订阅变化而漂移。改了**要留痕**，而且**分布数必须重新测量、不能按比例换算**。backlog **B83** |
| 本地 mod 装上了却没挂载 | 本地 mod 的挂载条件与 Workshop 不同；`-scripted_tests` 套件格式里**没有**存/读档指令。backlog **B32 / B39 / B47** |

---

## 7. 环境事实（路径）

| 项 | 路径 |
|---|---|
| 游戏本体 | `C:\Program Files (x86)\Steam\steamapps\common\Victoria 3` |
| 用户目录 | `%USERPROFILE%\Documents\Paradox Interactive\Victoria 3` |
| 启用列表 | 同上 `\content_load.json`（**是用户的配置，任何脚本都必须还原**） |
| 探针日志归档 | 同上 `\v3probe-logs-archive`（每局一个代表目录，其余进 `_obsolete\`） |
| 性能 CSV | 同上 `\ticktask_timings.csv` |
| 工具链 | `.venv\Scripts\v3.exe` · `python.exe` · `ruff.exe` · `mypy.exe` |
| 游戏侧判定产物 | `Documents\tests.txt` 与 `binaries\*_GameTests_testoutput.xml` |

**停机时的安全状态应当是**：`victoria3` 进程 **0 个** · `content_load.json` 是用户**原来的**配置 ·
仓库里没有本地 mod 目录残留 · `%TEMP%` 里没有遗留截图。

---

## 8. 文档地图（谁管什么）

| 文档 | 管什么 |
|---|---|
| `../01-大方向.md` | **冻结**：目标 · 原则 · 冻结清单 · 路线图 · 验收 · 实现期纪律 |
| `../01a-依据与参考.md` | 输入清单 · 候选池 · **手段阶梯** · 引擎实测 · 能力天花板 · 示例初判表 |
| `../backlog.md` | 所有悬而未决的问题（B 编号）+ 推荐值 + **触发条件** |
| `exec/阶段*-结果.md` | 各阶段**只留结论与证据**的结果文档（历史记录，**不回头改数字**） |
| `exec/收口清单.md` | 每批 / 每阶段收口 A→E（可复算） |
| `exec/自动化范式.md` | 实机范式、控制台手法、观察者局的三条副作用 |
| `exec/阶段7-长期维护.md` | 门禁全表 · 时间预算 · CI 矩阵 · 官方更新演练步骤 |
| `exec/阶段5-批次3-收尾交接.md` | **本批次**的位置与未完成项 |
| `exec/夜间进展.md` | 某一段夜间的连续工作时间线（历史） |
| `../../tools/README.md` | 工具链逐模块说明 · 命令表 · **无游戏环境矩阵** · 已知边界 |
| `../../README.md` | 项目总览 · 实测指标 · 目录结构 · 开发约定 |
| `../victoria3-modding/` | 20 篇 mod 开发知识库（**引用 `why` 的来源之一**） |

---

## 9. 本页的由来

2026-09-23 用户问「所有有用信息都记录下来，能让下次无缝继续吗」。当时的实情是：
**知识散在 30 份 exec 文档里，没有任何一页告诉新会话"现在在哪、下一步做什么"** ——
本页就是补这个缺口。审计时同步修掉了两处过期数字：`阶段7-长期维护.md` 里
用例数 `1427 → 1434`、引用数 `304 → 770`（都经 `v3 citations` / `pytest --collect-only` 实测）。
