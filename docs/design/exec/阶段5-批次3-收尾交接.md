# 阶段 5 批次 3 —— 收尾交接（临时记录）

> 这是一份**交接记录**，不是阶段结果文档。写它的原因是本次会话在"批次 3 已全部落地、
> 但还没有正式收尾"的位置被要求暂停。下次开工先读这一页，再决定要不要写
> `阶段5-批次3-结果.md`。

## 一、一句话状态

批次 3 的两份档案（**西班牙 `sp_empire_remnant`** + **巴伐利亚 `bv_alignment`**）数据源、
产物、门禁、用例**全部落地**，本页与它们一起提交。**没有半成品**：工作树里剩下的改动
都是已完成的工作，不含"改到一半"的东西。

## 二、本次实测已绿（证据）

| 门禁 | 结果 | 备注 |
|---|---|---|
| `v3 verify` | **234 / 234 ✅** | 本次把 `mod.total` 23→24、`mod.files` 4777→4828 两条断言连同 doc 12 的 claim 锚点一起更新（见 §四） |
| `v3 check-outputs` | **✅** | 先重跑 `v3 analyze` 刷新 `tools/out/*` 与 `tools/reports/*` 才对得上 |
| `v3 modguard` | **5 / 5 ✅** | 真游戏数据上全绿 |
| `pytest test_modguard.py` | **43 / 43 ✅** | 夹具补了批次 3 的词汇（见 §五） |
| `pytest test_golden.py` | **8 / 8 ✅** | |
| `v3 citations` | **770 ✅** | |
| `v3 modgen --write` | **60 个产物逐字节一致 ✅** | |

全量套件（`pytest tools/tests -q -n auto`）在**修完夹具之前**跑过一次：1425 passed /
8 skipped / **1 failed**（`test_cli.py::test_check_outputs_有产物时通过`，原因是产物
JSON 还停在 23 个 mod）。该失败的两条根因都已修掉（刷新产物 + 更新断言），并且
`test_check_outputs` 与 `test_golden` 已**分别复跑为通过**。**全量套件尚未在修完后整跑
一遍** —— 这是下次开工的第一件事（预计 4 分钟）。

## 三、下次从这里继续（按顺序）

1. **整跑一遍全量套件**：`.\.venv\Scripts\python.exe -m pytest tools/tests -q -n auto`
2. **写 `docs/design/exec/阶段5-批次3-结果.md`**（西班牙 + 巴伐利亚）。必须写进去的一条：
   巴伐利亚的**抽象缺口** —— "倒向谁"这个选择在现有能力里**表达不出来**，能表达的只有
   "倒向"这件事的**代价**。不要把缺口写成成绩。
3. **补记 `eg_debt` 的 A/B 结果**（上一批做的，还没落进文档）：改革窗口**开**✅、
   冲击**触发**✅、改革派**没有**入阁 ❌、clannish `intelligentsia b18` vs `landowners b25`、
   `ENACT none` 27/27。同时：新加的 `ZZPROBE AB;TARGET;<档案 id>` 自臂行**还没有在实跑的
   A/B 里复核过一次**（只验过代码路径）—— 下次跑 A/B 时顺手确认。
4. 阶段 5 剩余模式：**失去市场**。
5. 阶段 6：难度规则的**界面确认** + 人的 G3/G4 试玩。阶段 7：**B77**（citations 进 CI）、
   发布流程工具。阶段 4 ④：标准压力场景。

## 四、本次改掉的两条现场断言（要留痕）

`tools/pdx/verify.py` 的 `mod.total` / `mod.files` 断言的是**本机现场**（用户订阅的
Workshop mod 集合），现场变了它们就该跟着变。本次用户新订阅了一份 mod，于是：

- `mod.total`：23 → **24**（doc 12 第 3 行的 `<!--claim:mod.total-->` 锚点同步改）
- `mod.files`：4,777 → **4,828**（doc 12 第 38 行的 `<!--claim:mod.files-->` 锚点同步改）

**未做完的对账（记 backlog B83）**：doc 12 里还有一批**按 23 个 mod 快照算出来的分布数**
没有重算 —— 第 173/242/279/299/300/301/302 行的"23 个 mod"、版本号分布 `9/4/1/9`（加起来
正好 23）、`13/23 零覆盖`、`20/23 带 localization`、`1,456` 条本地化条目、`common/` 的
`10,379 + 807` 条条目、以及 §1 使用率表与 §2 的逐 mod 表。本次**只改了两条被断言看守的
数字**，没有盲目替换这些分布数 —— 因为它们必须**重新测量**而不是换算。

已经写好、放在 `%TEMP%\remeasure_doc12.py` 的一次性复测脚本可以算这些量，但它**当前是坏的**：
`m.files` 是 `int` 不是序列，`files.extend(m.files)` 直接抛 `TypeError` —— 下次要把
"文件清单"和"文件数"分清楚（先 `dir(ModInfo)` 看一眼字段名）再跑。

## 五、`test_modguard._fake_game` 的夹具代价（B82 又一次）

批次 3 把合成夹具又踩了一遍，这次补进来的是：国家 tag `SPA` / `BAV`、修正字段
`country_influence_mult`、`interest_group_ig_armed_forces_pol_str_mult`、
`interest_group_ig_petty_bourgeoisie_pol_str_mult`、JE 分组 `je_group_german_unification`、
触发器 `is_in_geographic_region`、图标 `event_carlist_beret.dds` / `waving_flag.dds` /
`modifier_rifle_negative.dds`。**闸门本身没有错，错的是夹具"按需列出原版有什么"这个设计**，
B82 里"让它从真实游戏目录自动取词条"的建议仍然值得做。

## 六、环境安全事实（关机前确认）

- `victoria3` 进程 **0 个**
- `content_load.json` = 用户原来的 **23 条 Workshop** 配置，没有残留的本地 mod 目录
- 探针产物（`tools/out/`、`tools/reports/`）是本次 `v3 analyze` 刷新的**当前现场**，可以直接提交

## 七、本次会话新增的 backlog 条目

- **B83**（本页 §四）：doc 12 的分布数仍基于 23 个 mod 快照，需重新测量而非换算。
