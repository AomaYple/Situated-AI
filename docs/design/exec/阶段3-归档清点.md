# exec · 阶段 3 实验归档清点（证据链 + 待清理项）

> 用途：阶段 3 的所有 A/B 实测原始数据都在用户目录
> `…\Documents\Paradox Interactive\Victoria 3\v3probe-logs-archive\` 下。
> 本文件把"哪一局、多少证据、什么结论"钉住，并列出**收口时必须清理的残留**。
> 复算：`v3 ab --logs "<归档目录>"`（或先 `--health`）。

## 一、阶段 3 的有效局（按结论分组）

| 归档目录 | 角色 | 证据量（`ROLE` 行） | 读数与结论 |
|---|---|---|---|
| `ab-A-control` | A 对照 | 41 | **合法性 16/16 月 >80**（分档 vhigh）——无冲击时的水位基线 |
| `ab-run2-done` | A 对照 | 95 | 40 个月：改革窗口**从未开**、法律全程 `law_serfdom` |
| `ab-run3-done` | B 处理 | 76 | 61 个月、`SHOCK 100%`：开窗判据 50 下**从未开窗**；合法性只在 70–80 徘徊（**会自愈**）→ 证明 -35 这一档压不到原版 50 线 |
| `ab-run8-window-open` | B 处理 | 110 | 判据换到 `≤75` 后：**窗口连续 24 月 `active`**；合法性下探到 65–70；策略牌由 `reactionary_agenda` 重抽到 `conservative_agenda`；**法律仍一条未换** |

**由此得到的三层结论**（写进 `阶段3-结果.md`）：
世界层（A 级修正确实生效）→ 策略层（能重抽，但落到"保守/兜底"）→ **行为层实质未动**；
瓶颈定位：**原版"改革派"牌的权重取决于它读到的输入（工业家/知识界是否强势），而冲击只压了贵族与合法性。**

## 二、同一会话的重复快照（清理候选）

`--watch` 每 40 秒按版本快照，因此同一局会出现多个目录，内容重叠：

* `ab-run2`（713）与 `ab-run2-done`（95）——同一会话；
* `ab-run3`（330）与 `ab-run3-done`（76）、`ab-run4` / `ab-run4-partial`；
* `ab-run5` / `ab-run6` / `ab-run7`——判据校准前后的同一串会话；
* `ab-A`（188）/ `ab-A2`（215）/ `ab-batch`（106）——早期会话，含废局。

**处理**：收口时只保留"每局一个代表目录"（上表第一组）+ 需要复算的最小集合，其余移到
`…\v3probe-logs-archive\_obsolete\`（**不直接删**，留一次翻查机会），并在提交信息里写明。

## 三、收口时必须清理的游戏侧残留

| 项 | 现状 | 处置 |
|---|---|---|
| `game\tools\scripted_tests\sitai_ab.txt` | **我为验证"引擎是否只读安装目录套件"而放入**（该假设已被证伪） | **删除**，恢复原版 5 个套件（`germany / ip3 / italy / springtime / test` + `scripted_tests.md`） |
| 用户 mod 目录 | `zz_probe_ab` + `sitai_ru_defeat`（实验用） | 阶段 3 收口后 `v3 experiment restore` + 卸载探针，还原用户原 23 个 mod 的启用列表 |
| `%TEMP%` 截图 | `v3-screen*.png` / `v3-now.png` / `v3-topright.png` / `date*.png` | 删除 |

## 四、下一轮实验的判据（**预注册**：先写死，跑完不许改）

自动化流水线已能"一次点火换一整套数据"，下一局按臂阶梯跑：`RUN;A`（1–12 月，无冲击）→
`RUN;B`（13 月起施加冲击）→ `RUN;B2`（37 月起追加改革侧输入：工业家/知识界政治力量上调、贵族继续压低）。

**开跑前先把判定写死**，避免跑完再挑解释：

| 观察 | 判定 | 处置 |
|---|---|---|
| B2 段出现**法律转换**（`LAW` 从 `law_serfdom` 变成别的） | **H2 成立**（行为层实质变化） | 补一次同向复现（换切换日期）后收口阶段 3 |
| B2 段窗口开、策略牌落到**改革议程**，但法律不变 | 瓶颈在"牌 → 立法行动" | 记录为**阶段性负结果**：意图层能改世界与选择，但改不动立法；进 `backlog`，按冻结文档重估该档案的做法 |
| B2 段与 B 段无差别（策略牌仍落保守/反动） | 我们给的输入相对原版权重太弱 | 用与"合法性分档"同样的办法**先量 IG 势力实际值**（分档日志），再决定是否加码——**不许盲调数字** |
| A 段（对照）也出现开窗或换法 | 实验被污染（漂移/残留） | 该局作废，检查臂阶梯的施加是否幂等、日志是否分段正确 |

**口径提醒**：判据只看**行为层**（JE 状态、法律、IG 势力）；策略层（三槽落点）只作参考——
执行文档写死了"只有策略层动 = H2 不成立"。



## 五、口径提醒（避免后人误读）

* 本阶段的冲击（`sitai_ru_defeat_pressure`：合法性 -35 / 贵族 -0.30 / 利率 +0.35）是**实验用直接修正**，
  **不是正式版设计**；正式版应由真实触发（输掉战争/割地赔款）+ 因果链导出（记 `backlog`）。
* 开窗判据 `legitimacy <= 75` 是**按实测分位校准的实验值**，正式档案必须按设定重新论证。
