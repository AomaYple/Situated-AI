# exec · 阶段 3 重做 结果：意图层通道打通了，「牌 → 法」还差一环

> 判据**在跑之前**写死在 `阶段3重做-执行.md`（预注册），本文件只补读数与结论，不改判据。
> 机制依据：`backlog.md` §三·补③（B53–B61，每条带 `文件:行号`）。
> 跑法：`tools/probe/stage3_rerun.py --months 144`（一局实机，观察者局 + 自励冲击）。

## 一、结论先行

| # | 预注册判据 | 结果 | 读数 |
|---|---|---|---|
| **P1** | **法律真的换了**（H2′ 的唯一实质判据） | ❌ **没有** | 全程 **146 个月**只有 `LAW;law_serfdom`，**0 次转换** |
| **P2** | 牌闸门成立（进步牌挂上过） | ✅ **成立** | `STRATEGY;ai_strategy_progressive_agenda` 出现 **3 次**（三开三关） |
| **P3** | 窗口开过 | ✅ **开过** | `JE;active` ↔ `inactive` 完整切换 **2 轮** |
| **P4** | 冲击施加过（阳性对照） | ✅ **施加过** | `SHOCK;yes` ↔ `no` 完整切换 **2 轮** |
| **P5** | 命名空间内 0 报错 | ✅ | `error.log` 对 `sitai` / `je_sitai_` **0 命中** |

**一句话**：**「窗口一开就递原版策略牌」这条机制在原版引擎里真的生效** ——
`immediate` 块对 **AI 国家**执行、`on_complete` 也执行，开与关都留了逐月读数。
**但牌换了 12 年，农奴制一条没改。**

⇒ 阶段 3 上一次**连臂阶梯都没跑起来**（`阶段3-结果.md` §六），这一轮把那条结构阻断
解开了，并且拿到了上一次完全没有的东西：**牌与窗口的逐月读数与开关闭环**。
H2′ 仍然**不成立**，但失败的**位置**从"意图层根本没通电"挪到了"牌 → 法"这一段。

## 二、原始读数（引擎自己写的 `debug.log`，不是我们的自报）

```
分类计数 : {'ROLE': 146, 'STRATEGY': 146, 'JE': 146, 'LAW': 146, 'SHOCK': 146,
            'LEG': 146, 'INPUT': 146, 'POLI': 146, 'ADMI': 146, 'DIPL': 146,
            'SELFARM': 2, 'RUN': 2}
法律读数（去重，按出现顺序）: ['law_serfdom']          ← 146 个月只有这一条
策略牌读数（去重，按出现顺序）: ['progressive_agenda', 'reactionary_agenda',
                                'progressive_agenda', 'reactionary_agenda',
                                'progressive_agenda', 'reactionary_agenda']
窗口读数（去重）: ['active', 'inactive', 'active', 'inactive']
冲击读数（去重）: ['yes', 'no', 'yes', 'no']
```

读法（三条都是**新**能力，上一次拿不到）：

1. **递牌真的生效且真的回退**：`progressive ↔ reactionary` 与 `active ↔ inactive`
   **一一对应**，切换 3 轮。⇒「窗口开 ⇒ 换路线」「窗口关 ⇒ 递回去」两条都成立。
2. **自励路径可用**：`SELFARM: 2` + `RUN: 2` —— 观察者局里**不点决议**也把冲击施加了
   （阶段 3 的臂阶梯就是死在"没有玩家国家 ⇒ 决议点不了"）。
3. **世界状态那条链也在**：`SHOCK;yes ↔ no`（10 年修正到期）+ 合法性全程 `b55`。

## 三、为什么"牌换了法还不换"——**已查明：政府里只有地主**

第二局（`--months 96`，加了 ENACT / GOV 两行分辨器）跑到 **1844.1（≈96 个月）**，
读数**全部同向**：

```
AB;ENACT;none;俄罗斯         96 / 96 —— **一次都没开过立法**
AB;GOV;landowners;俄罗斯     96 / 96 —— 政府里**只有地主**
（intelligentsia / industrialists 一次都没出现过 —— 它们从未进入政府）
AB;LAW;law_serfdom;俄罗斯    222 —— 法律没变
AB;JE;active;俄罗斯          163 —— 窗口大部分时间开着（inactive 25）
AB;STRATEGY;progressive_agenda  102 —— 进步牌是主态
AB;STRATEGY;conservative_agenda  28 —— 原版自己重抽到保守牌 28 次
AB;STRATEGY;reactionary_agenda   58
AB;SHOCK;yes;俄罗斯          164 —— 冲击在（no 24）
零报错：error*.log 对 `sitai` 0 命中
```

⇒ **候选「甲 · 政府支持不足」成立**（原版读本 `common/laws/readme.md:9-10` 点名的正是这条）：

* **AI 一次都没开始推那条法**（`ENACT;none` 96/96）—— 不是"开了没成"，
  而是**根本不动手**；候选乙（怕革命）据此**排除**：怕革命也会先开立法再被打断；
* 政府里**只有 `ig_landowners`** —— 而地主正是 `law_serfdom` 的守旧方，
  也是 `law_tenant_farmers` 的反对方（那条法会把地主政治力量 −0.25，
  `01_land_reform.txt:155`）；
* 我们那把"改革派抬头"的修正（知识界/工业家 `pol_str_mult +0.5`）**没能把它们送进政府**
  —— `+0.5` 抬的是 clout 份额，而"在不在政府里"是另一个判定。

**因此瓶颈不在牌，也不在权重，而在"改革派有没有进入政府"。**
这是 `01-大方向.md` §0.1 那句"最优 = **约束下的**可行最优"的字面体现：
AI 的处境已经变了、它的目标函数已经指向改革，但**约束集（谁在朝）不允许它动手** ——
于是它什么都不做，而这恰恰是"按约束推出来的最优"。

⚠️ **半个保留态已关掉（第三局，逐条问 20 条候选法）**：`ENACT` 从"只盯两条土地法"
扩成**逐条问 20 条候选法**（土地改革整组 / 权力分配整组 / 征税 / 奴隶制 / 教育），
跑 **169 个月**，读数：

```
AB;ENACT;none;俄罗斯        169 / 169 —— **20 条候选法一条都没在推**
AB;GOV;landowners;俄罗斯    169 / 169 —— 政府里**只有地主**
零报错（error*.log 对 sitai / Unknown trigger / is_enacting 全 0 命中）
```

⇒ 「它在推别的法」这个解释**被排除**：「甲 · 政府支持不足」是一个**结论**，不再是假设。
（为什么只能逐条问：**没有「任意法」的通用写法** —— exe 检索
`is_enacting_any_law` / `has_any_enactment` / `any_enacting_law` / `enactment_progress` /
`current_enactment` 全部 0 命中，只有 `is_enacting_law` 存在，2 命中。）

## 三·补、三个候选的裁决（把执行文档的分支填上）

| 候选 | 裁决 | 依据 |
|---|---|---|
| **甲** 政府支持不足 | ✅ **成立（结论）** | `GOV;landowners` 169/169（改革派 IG 从未在朝）；`ENACT;none` 169/169（**20 条候选法一条都没在推**） |
| **乙** 怕革命 | ❌ 排除 | 怕革命的表现是"开了立法再被打断"，而这里是**从未开** |
| **丙** 权重不够 | ❌ 排除（本轮范围内） | 权重不足会让 AI **去推别的法**，而不是 20 条候选法一条都不碰 |

## 三·补②、这一局的另一条读数：**窗口与牌在正常地开合**

```
策略牌（按出现顺序）: reactionary → progressive → conservative → reactionary
                     → progressive → conservative
窗口              : inactive → active → inactive → active
冲击              : no → yes → no → yes
法律              : law_serfdom（169 次，一次没变）
```

⇒ 「窗口开 ⇒ 换路线」「窗口关 ⇒ 递回去」两条**都在动**（三轮），而且原版自己重抽到
**保守牌**（`conservative_agenda`）确实发生过 —— 与阶段 3 上次那条负结果一致，
说明**递牌不是"按住不放"**：AI 仍会按自己的权重重抽，我们只是把落点拨过去一次。
这一点对设计有意义：**递牌是"推一把"，不是"锁死"**。

## 四、这一轮真正拿到的（可以往下用的）

1. **生成器有了递牌能力**：`[journal_entry.signals]` → JE 的 `immediate` / `on_complete`，
   只许递**原版**牌（白名单），带往返净度与 6 条用例。
2. **观察者局能自励**：`zz_probe_ab_selfarm`（`is_ai = yes` + 决议优先守卫），
   解开了阶段 3 的结构阻断（B61 的第一步）。
3. **牌是一级读数**：`ZZPROBE AB;STRATEGY;` + `pdx.ab` 的政治牌表与差分。
4. **一局实机能跑满 146 个月**（12 年）且**全程 0 报错**；收尾（还前台 / 杀进程 /
   还原 `content_load.json` / 删本地 mod）全自动，用户原来那 23 条 Workshop 一个字没动。

## 五、下一步（按 01-大方向 §5 的纪律：先证判据，再谈功能）

### 5.1 判定已做完：**「入阁距离」是可观测的**（第四局）

上一轮留下的问题是"改革派离入阁有多远？" —— `is_in_government` 只有真/假，答不了。
这一局给探针加了 `CLOUT` 读数：用 `ig_clout`（引擎触发器，
`trigger_localization/00_trigger_localization.txt:2936`）按五档夹逼
`0.03 / 0.08 / 0.12 / 0.18 / 0.25`（后两档直接对应 `is_powerful` 那条线：
`defines/00_defines.txt:189` `POWERFUL_IG_THRESHOLD = 0.20` / `CUTOFF = 0.18`）。

**跑 56 个月，读数（每月每 IG 一行「≥档位」，水位取出现过的最高档）：**

| IG | 水位 | 逐档出现次数 | 读法 |
|---|---|---|---|
| `landowners` | **≥ 0.25** | 56 / 56 / 56 / 56 / 56 | **全程稳稳的 `is_powerful`**，最强势力 |
| `intelligentsia` | **≥ 0.18** | b03 56、b08 56、b12 49、b18 **9** | 有点分量、**偶尔**碰到"强势"线 |
| `industrialists` | **≤ 0.03** | b03 **49** | **政治上几乎不存在** |

⇒ 三件事同时成立：

1. **入阁距离是可观测的** —— 五档夹逼把"改革派有多弱"变成了趋势（与合法性同一手法）；
2. **工业化在前 5 年确实还没起来**（工业家 clout ≤ 3%）—— 这**支持**"现在还不该改革"
   这一判断：AI 不推改革，**在它的约束集里是理性的**；
3. **智力界已经摸到过 0.18**（9 个月）—— 说明"改革派进政府"**不是天方夜谭**，
   差的是**持续性**（56 个月里只有 9 个月够得着那条线）。

⚠️ 一个读数陷阱（实测踩到，已修）：`CLOUT` 是「**≥**档位」的**单调**读数，
水位只能取**最高档**，不能取"出现次数最多的那一档" —— 按"最多"取会得到 `b03`
（看起来智力界几乎没有力量），而它其实有 9 个月到过 0.18。
这与 `pdx.ab` 里 `leg_top` 那个坑是同一类。

### 5.2 于是下一步是**设计选择**，不是继续调参

数据说的是：**改革派的 clout 比地主低一个数量级**（≥0.25 vs ≤0.03~0.18），
而这是**真实世界状态**（工业化没起来、识字率低）。所以有两条路，**必须选一条**：

* **路 A · 承认约束**：前 5 年不改革**就是**这个处境下的最优解 ——
  那么本档案的产出是**意图位移**（窗口 / 牌 / 三行解释）而不是"真的换法"，
  验收口径相应改成"**意图可读 + 世界层真实**"，把"真的换法"留给**工业化之后的处境**
  （另一份档案）。这与 §0.1「约束下的可行最优」**完全一致**；
* **路 B · 让约束真的松动**：战败 → 赔款 → 财政危机 → 地主势力**真的**受损。
  现在 `[pressure]` 里的 `interest_group_ig_landowners_pol_str_mult = -0.3` 是
  **实验用的直接修正**，实测说明它**远不够**（地主仍是 ≥0.25）。
  走路 B 就得按原版量级来（原版同族最大档 **+2.0**，
  `static_modifiers/content_304_modifiers.txt:228`），并**给出因果链**
  （为什么战败会让地主掉这么多：农奴制下的财政 / 征兵 / 土地关系）。

**先走路 A**：它不需要新的数字论证，直接对应文档口径，而且**已有实测支撑**
（"AI 不动手在它的约束集里是理性的"）；路 B 需要重新论证每一个数字（P10），
值得做，但要先把路 A 的验收做扎实。

### 5.3 G3 的可核对化：三行拆成三个键（本轮的第三件产出）

G3 判的是「试玩者能复述**当前目标 + 主因**」。在此之前三行揉在 JE 的 `_reason` 一段散文里
—— 复述不出来的时候**分不清是哪一行没写清楚**。现在拆成三个独立本地化键
（`<JE 名>_goal` / `_pressure` / `_last_change`），**缺一行当场报错**：

* 数据源新增可选表 `[panel]`（三行要么都写、要么整表不写）；
* 文案仍走同一个 `localization` 列表（单一数据源，P9），每行各带 `why`（P10）；
* 档案文档新增「面板三行」一节，逐行列出键名与依据；
* 三行按**判据**写、不按机制写：目标行不写"废除农奴制"（改哪条法由 AI 自己推），
  压力行与 `[pressure.effects]` / `complete` 各自对应，**上次改主意的原因**单列一行
  —— 没有它，玩家只能看到"它在改革"，看不到"为什么是现在"。

### 5.4 出口判据「和平期占槽率 ≈ 0」：**实测通过**

这条判据不能靠"看着没出现"来判，也**不能带着探针测**（探针自己会写变量、写日志；
而且"日志里没有那一行"在一份只有探针的日志里是**沉默**，不是证据）。

测法：`stage3_rerun.py --no-probe --fresh-logs`（只装真 mod + 开局前把旧日志挪走）。

```
已装本地 mod：sitai_sitai
已把 60 个旧日志挪去临时目录（这一局的读数只可能来自这一局）
跑到 1839.1.31（≈37 个游戏月）
总行数 : 0        ← 一条 ZZPROBE 行都没有（没有探针，也没有自报）
```

**硬证据（同一局）**：

* `Mounted Data: …/mod/sitai_sitai` —— 我们的 mod **确实被挂载**（不是"没装上所以没效果"）；
* `error*.log` 对 `sitai` / `je_sitai` **0 命中**（整局 1673 行报错里没有一条是我们的）。

⇒ **37 个月、没有任何冲击的世界里，改革窗口一次都没出现** —— 与 `is_shown_when_inactive`
的判据（要 `has_variable = sitai_ru_defeat_memory`）一致。

⚠️ **踩过的一个假红（写下来免得再踩）**：第一次跑 `--no-probe` 时没挪旧日志，
读数里出现 14 条 `JE;active` —— 那是**上一局**留在 `debug.1.log` 里的尾巴。
"不带探针的一局没有自报"这件事，只有在**这一局的日志集里**看才成立。

### 5.5 阶段 3 重做的出口判据：逐条结账

| 出口判据（`01-大方向.md` §3 阶段 3） | 状态 | 证据 |
|---|---|---|
| **G2 差分可复现** | ⚠️ **部分** | 世界层与意图层的差分**可复现**（窗口开/关、牌换/递回，四局一致）；**行为层（换法）不成立**，并已查明原因是**约束集**（政府里只有地主，clout ≥0.25 vs ≤0.03~0.18） |
| **G3 三行说得通** | ✅ **可核对化达成** | 三行拆成三个独立键（§5.3），缺一行**报错**；每行按判据写、各带 `why`。**"试玩者能复述"这一条要人来判** —— 我不替试玩者宣布它通过 |
| **和平期占槽率 ≈ 0** | ✅ **通过** | §5.4：37 个月、mod 已挂载、命名空间 0 报错、窗口一次没出现 |

⇒ 阶段 3 重做**可以收口**，但它**不是"H2 成立"**：按 `01-大方向.md` §3 的失败处理
（"差分不出现 → 换失败模式或换挂载点，不加代码"），本轮**换了挂载点**
（世界状态 → 递牌），拿到了机制层面的确定结论；下一步的选择写在 §5.2（路 A / 路 B）。

### 5.6 无论走哪条，这一轮的产物都留下

递牌能力（生成器）、自励路径（探针）、**六类**逐月读数
（牌 / 窗口 / 立法 / 政府 / clout 水位 / 合法性档位）、面板三行、以及
"跨会话日志残留"这个假红教训 —— 它们让"下一步为什么失败"每次都**当场可辨**。
