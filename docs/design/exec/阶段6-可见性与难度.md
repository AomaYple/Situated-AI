# exec · 阶段 6：可见性与难度（一页执行文档 + 已做/未做）

> 依据：`01-大方向.md` §3「阶段 6」三行。
> **目标**：三行解释上屏（JE/tooltip → 必要时 scripted GUI）、难度三档 `game_rules`、中英本地化。
> **出口判据（G3/G4）**：试玩者能复述「目标 + 主因」；反馈关键词是「**世界活了**」。
> **失败处理**：解释不清 → 加第四行（身份项来源）。

## 一、已经做完的部分（2026-09-23）

| 项 | 状态 | 证据 |
|---|---|---|
| **中英本地化** | ✅ 每份档案各出一份 `l_english` / `l_simp_chinese`（生成物） | `v3 modgen` 30 产物；闸门 ② 逐键核对本地化 |
| **三行拆成三个独立键** | ✅ `<JE 名>_goal` / `_pressure` / `_last_change` | `PANEL_LINES`；缺一行当场报错（`v3 modgen` 退 2） |
| **三行真的上屏** | ✅ 三行**接进 JE 的说明**（`<JE 名>_reason`） | 见 §二：引擎显示的那一处是 `journal_entry.gui:742` 的 `text = "[JournalEntry.GetReason]"`；`v3 modgen` 现在**要求**数据源里有 `_reason`，缺了就报错（不然"没上屏"是静默的） |
| **四份档案里的哪几份有三行** | ⚠️ 只有 `ru_defeat` 与 `cn_intervention` | `tr_defeat` 与 `au_revolution` 没有 `[panel]` ⇒ backlog **B70** |

## 二、两条**有据可查**的界面事实（决定了怎么上屏）

1. **JE 说明来自 `<JE 键>_reason`**：`game/gui/journal_entry.gui:742` 是
   `text = "[JournalEntry.GetReason]"`。这是"三行要放哪儿"的**唯一硬依据**。
2. **`<JE 键>_goal` 那一槽不保险**：引擎会为我们写的 `_goal` 报
   `journal_entry_type.cpp:476 Journal entry has redundant loc for <JE 键>_goal`
   （实测 2026-09-23，日志 `game.*.log`）。原版确实大量使用 `je_*_goal`
   （如 `je_acw_reconstruction_goal`），但那句 `redundant` 说明"这一槽到底显不显示"
   取决于 JE 的形态 —— **没有把握的事不当判据**。所以：三行**也**接进 `_reason`，
   三个独立键照旧保留（能单独核对；将来若做脚本化 GUI，三个键可直接接到三行控件上）。

> 结论口径：**"上屏"以 `_reason` 为准**（有 GUI 代码为证），`_goal` 只当加分项。

## 三、还没做的部分

| 项 | 为什么没做 | 下一步 |
|---|---|---|
| **难度三档 `game_rules`** | 需要新增一类产物（`common/game_rules/*.txt`）+ 数据源 schema 的 `[difficulty]` 表 + 闸门 ② 的新引用类型；这是**生成器的结构性扩展**，不该塞在收尾批次里顺手做 | 单开一批：schema 先定（三档各自改哪些字段/哪几条压力强度），再落生成器与用例 |
| **`scripted GUI`（"必要时"）** | 上面第 2 条已经让三行上了屏 —— 按三行原文"**必要时**"，现在**不必要** | 只有当"三行挤在说明里"读不清时才做；那时三个键已经就位，直接绑控件 |
| **G3 实机验证（试玩者能复述"目标 + 主因"）** | **需要人**（试玩者）。自动化能给的是"文案在屏幕上、三行都在"；"能不能复述"是人的判据 | 一次人工试玩；反馈关键词按原文记（"世界活了"） |
| **G4 不退化** | 同上，需要试玩或对照会话 | 与阶段 5 的 A/B 会话合批做 |

## 四、复算（复制即用）

```powershell
.venv\Scripts\v3.exe modgen --write      # 生成（含本地化与档案文档）
.venv\Scripts\v3.exe modgen --check      # 产物 = 生成结果
.venv\Scripts\v3.exe modguard            # 五道闸门
# 看某份档案的说明里有没有三行（`\n\n` 是字面量，两个字符）：
Select-String -Path mod\localization\simp_chinese\sitai_cn_intervention_l_simp_chinese.yml -Pattern "_reason"
```

## 五、边界

* 本阶段**不做**：阶段 7 的长期维护流程（版本演练 / CI / 发布）；
* 文案改动一律**改数据源**（`mod/data/*.toml`），产物由生成器落盘 —— 手改产物闸门 ⑤ 会红。
