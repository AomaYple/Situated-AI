# exec · 阶段 5 批次 1 结果：两份**非战败**档案（革命潮 / 列强干涉）

> 依据：`01-大方向.md` §3「阶段 5」三行 + `exec/阶段5-档案扩张.md`（一页执行文档）。
> 每批 2–3 个档案，走「数据源只加行 → 生成 + 五道闸门 → 基准 + gametimer → 一次 A/B 会话 → 收口」。

## 一、这一批加了什么（**只加数据行**）

| 档案 | 主角 | 失败模式 | 它与"战败"哪里不同 |
|---|---|---|---|
| `mod/data/au_revolution.toml` | 奥地利（`AUS`） | **革命潮** | 记忆记的是"经历过革命危机"；压力侧第一次用 `country_radicals_from_legitimacy_mult`（合法性塌 → 激进派涨，**这是革命潮的机制本身**）；JE 用原版革命危机那一对 `je_group_crises` + `event_protest.dds` |
| `mod/data/cn_intervention.toml` | 大清（`CHI`） | **列强干涉 / 被迫开国** | 记忆记的是"被列强干涉过"；守旧侧换成 `interest_group_ig_devout_pol_str_mult`（大清的守旧侧是士绅与旧官僚，不是地主贵族）；JE 多一条 `country_has_primary_culture = cu:manchu`，把窗口锁在满清统治形态上 |

**两份都一张牌都不递**，理由写在各自的 `[cards].why` 里，而且是**判据性**的：
`ru_defeat` 的递牌写法要引"该国 1836 年的初始牌"，而奥地利在原版
`common/history/ai/00_strategy.txt` 里**没有 `c:AUS` 块**（只有 `c:RUS` 与 `c:TUR`）——
照抄俄国的三行等于**替 AI 选路**，那违反 §0「让 AI 自己推导」。
**查不到初始落点就不递牌**，比递一张编的牌诚实。

## 二、出口判据逐条（照 `阶段5-档案扩张.md` §三）

| # | 判据 | 本批读数 |
|---|---|---|
| G-EXIT-5.1 | 每档案带**判据与 `why`** | ✅ `v3 modgen` 编译 4 份档案 / **30 个产物** / **50 个带依据的数字**；空 `why` 在编译期即退 2；`v3 modgen --why` exit 0 |
| G-EXIT-5.2 | 和平期占槽率 ≈ 0 | ⏳ **尚无读数**（两份档案都不递牌 ⇒ 槽位占用在闸门 ③ 里报 **0 张牌 / 0.0%**；但"和平期 JE 不开"要 A/B 会话，未跑） |
| G-EXIT-5.3 | 危机期上升 | ⏳ **尚无读数**（同上：需要一次 A/B 会话，未跑） |
| G-EXIT-5.4 | G4 不退化 | ⏳ **尚无读数**（需要试玩者/会话；本批只做到"能编译、能过闸门、引用全指得到"） |
| G-EXIT-5.5 | 抽象没被特例污染 | ✅ **只加了 2 个数据文件**；改了生成器**一处**（见 §四）并在此说明理由 |

**闸门**（`v3 modguard`，4 份档案 / 30 个产物）：① 键与路径与原版零交集 ✅ ② 引用完整性
（6 类逐个核过、零缺失）✅ ③ 稀释预算 **0 张牌 / 各槽 0.0%**（预算 60%）✅ ④ 往返净度
（无丢失、无多余字段）✅ ⑤ 两次生成逐字节一致 + `why` 全非空 + 盘上产物 = 生成结果 ✅。

**证据链**（这一批新增的那一条）：`v3 citations` —— **292 条 `文件:行号` 引用全部指得到唯一一行** ✅。
它上线第一件事就是**抓出既有档案里 5 处指不到真实文件的引用**（其中 `00_sikh_empire.txt:20`
那份文件**根本不存在**，真名是 `04_sikh_empire.txt`；另 4 处少了 `common/` 前缀），
以及**一句事实错误**（见 §四）。**已全部更正**。

## 三、口径与"没查实"的部分（诚实项）

* 两份档案的数值（`-35` / `-0.30` / `0.35` / `0.5` / 门槛 `75` / `days 3650` / `weight 100`）
  **全部沿用 `ru_defeat` 的阶段 3 实测档位**，每条 `why` 末尾都标了「未针对本条处境独立校准」。
  唯一独立取档的是 `au_revolution` 的激进派系数 `0.25`（依据是原版档位本身
  `00_ip4_04_modifiers.txt:177`），但它对奥地利的效果大小**没有实测**。
* **没跑的**：`v3 modgen --write` 之后的标准流程第 3、4 步（基准 + `gametimer` + 一次 A/B 会话）。
  因此 G-EXIT-5.2/5.3/5.4 全部记为"尚无读数"，**不许读成已达成**。
* **做不进去、如实记下的一条**：`au_revolution` 想要的"**激进派占比门**"在本 schema 里表达不出来，
  两条原因（原版没有国家作用域**平铺**的激进派触发器；生成器的 `Clause.render` 只支持平铺一行）
  写进 backlog **B69**。这一批的处理是**让步**：激进派状态由 `[pressure]` 字段承担（引擎自己的
  公式读它），门只开在合法性上 —— 照三行原文，这属于"抽象不够"而不是"该加特例"。
* `au_revolution` 与 `tr_defeat` 一样**没有 `[panel]`**（P11 三行）⇒ 记 backlog **B70**，
  阶段 6 开工第一件事补齐（三行是 G3 判据的载体）。

## 四、抽象改进与随之更正的两处事实（G-EXIT-5.5 要求写明）

1. **生成器里写死的"战败"措辞被参数化**（`modgen.py`：产物注释头 / 压力修正注释 / `Memory` 文档串）。
   原来它往**每份**产物注释里写"战败求存 / 战胜方也记成战败 / 战败压力"，
   于是革命潮与列强干涉的产物注释是**错的** —— 而"只生成不手写"之下人改不了产物，
   只能改生成器。现在注释由**档案自己的 `title`** 说话。**这属于抽象改进，不是特例分支。**
2. **`country_loan_interest_rate_add` 的一句断言是错的**：`ru_defeat` / `tr_defeat` 都写
   「原版**只出现过一次**」。实测（1.14.3，`common/`）**12 处**：全局基准
   `00_code_static_modifiers.txt:12 = 0.2`、法律侧 `00_amendments_enactment_04.txt:644/1138/1174/1222/1266
   = 0.02/0.02/0.03/0.01/0.05`、科技侧 `30_society.txt:209/599/1226/1473/1647 = -0.02`、
   字段定义 `00_modifier_types.txt:662`。「**国家静态修正**那一侧没有档位阶梯」仍成立。
   **两份档案的 `why` 都已改成实测事实**（记 backlog B72）。教训：`v3 citations` 只能查
   "引用指得到"，**查不出"计数断言"** —— 要真防住还得再有一条计数检查，未做。

## 五、复算（复制即用）

```powershell
.venv\Scripts\v3.exe modgen            # 编译（不写盘）+ 摘要：4 份档案 / 30 个产物 / 50 个数字
.venv\Scripts\v3.exe modgen --check    # 盘上产物 = 生成结果
.venv\Scripts\v3.exe modgen --why      # 每个数字 + 它的依据
.venv\Scripts\v3.exe citations         # 292 条 文件:行号 引用是否都指得到唯一一行
.venv\Scripts\v3.exe modguard          # 五道闸门
```

## 六、下一批（照 §一 的清单，不另造）

`阶段5-档案扩张.md` §一 的清单还剩：**破产 / 被包围 / 失去市场**，以及 `01a` §K 里
"倒向谁"那一类（德意志中小邦、意大利诸邦、印度土邦、奥斯曼属国，见 backlog B22）。
筛选标准只有一条：**该档案能否挂上判据**。下一批仍按 2–3 个一份走，并把
**A/B 会话（G-EXIT-5.2/5.3）** 补上 —— 本批欠的就是它。
