# exec · 阶段 5 实机读数补记（A/B 欠账）

> **为什么单独一页**：批次 1–3 的结果文档里，`eg_debt` 的 A/B 读数从没写进任何文档，
> `sp_empire_remnant` 与 `bv_alignment` **一次都没跑过**（G-EXIT-5.3 对它们是空档）。
> 这一页把补上的读数集中记账。
>
> ⚠️ **不改历史文档**：`阶段5-批次*-结果.md` 是历史记录（只留当时的结论与证据）。
> 本页是**补记**，读的时候按"后来补测的"理解。
>
> ⚠️ **读本页前先看 §五**：分析器在读轮转日志时**顺序错过一次**（已修）。
> 受影响的那一局（`bv_alignment`）在这里**逐格标了"稳 / 不稳"** —— 不许把不稳的格子当结论。

## 一、`sp_empire_remnant`（2026-09-23，66 个游戏月，**整段读数**）

```powershell
.venv\Scripts\python.exe -X utf8 tools\probe\stage3_rerun.py --months 66 --archive sp_empire_remnant --fresh-logs
```

tick 走到 **1841.7.24.12**（≈66.8 个月；进局速率 3.555 天/秒）。自报行 **盘上 1,401 行、
全部计入**（`--whole-log`，理由见 §五）。观测块 **67** 个（第 0…66 月）。

| 判据 | 读数 | 说明 |
|---|---|---|
| 冲击读数 | `['no', 'yes']` —— 第 **13** 个观测起为 `yes` | 与阶梯的 B 臂起点（第 13 月）**逐月对上** |
| **窗口读数** | `['inactive', 'active']` —— 第 **0–12** 个观测 `inactive`，第 **13** 个起 `active` | **本次最有价值的一条**，见下 |
| 改革侧输入 | `['no', 'yes']` —— 第 **36** 个观测起为 `yes` | 与 B2 臂起点（第 37 月）对上 |
| 进步牌挂上过 | ❌ 全程只有 `ai_strategy_conservative_agenda` | 意图层没换牌 |
| 立法开过 | ❌ **`ENACT none` 67/67** | 一条候选法都没推 |
| 改革派进过政府 | ✅ 在朝在 `landowners` ↔ `intelligentsia` 之间**来回 23 次**（前 12 个月全是 landowners） | **四批里第一次读到改革派入阁** |
| 各 IG 的 clout 水位（最高档） | `intelligentsia b18` / `industrialists b08` / `landowners b25` | |
| 本档案盯的法 | `law_latifundias` | `[probe].reform_law`（B80） |
| **那条法换过没有** | ❌ **`False`** | 到 1841.7 还是 latifundias |
| **引擎侧判定** | ❌ **不通过** | `suite_tests=2 / suite_failures=1`；挂的是 `law_changed` |
| `error.log` 里我们的报错 | **0 条** | `suite_our_errors = 0` |

### 1.1 为什么这一局的「窗口先关后开」是本阶段最硬的一条读数

四批档案里，这是**第一次看到窗口由关到开**，而且时间点**精确到月**：

```text
观测 0–12   （A 臂，对照）  冲击 no      窗口 inactive
观测 13–35  （B 臂）        冲击 yes     窗口 active     ← 冲击落地的那一个月就开了
观测 36–66  （B2 臂）       输入 yes     在朝 IG 开始来回换
```

它一次性回答了三件事：

1. **`legitimacy <= 75` 这条门槛在西班牙身上真的会成立**。这是整个档案里最容易
   **静默关死**的一条：门槛定高了，JE 就是不出现，引擎一个字都不报，而"没开窗"与
   "没施加冲击"在日志里长得一模一样。现在它是读数。
2. **`c:SPA ?= this` 这个平铺写法在引擎里生效**（窗口开了 ⇒ `is_shown_when_inactive` 成立）。
3. **因果链有先后**：不是"施加了冲击、窗口也开着"这种同时性的巧合，而是
   **第 13 个月冲击落地、同一个月窗口打开** —— 阶梯的两臂在读数上是分开的。

⚠️ 与它并列的**反面读数同样重要**：进步牌没挂上、立法 `none` 67/67、目标法没换、
引擎判 `law_changed` 失败。也就是说这一局是「**世界状态真的被改动了、窗口真的开了、
改革派真的入阁了 —— 但 AI 一条法都没推**」。这与批次 1–3 记的天花板（B53–B60）**一致**，
本局没有改变它。

## 二、`bv_alignment`（2026-09-23 **重跑**，66 个游戏月，**整段读数**）

```powershell
.venv\Scripts\python.exe -X utf8 tools\probe\stage3_rerun.py --months 66 --archive bv_alignment --fresh-logs
```

tick 走到 **1841.7.30**（≈66.9 个月）。**盘上自报 1,474 行、全部计入**（68 个观测块）。

**这一节是一次「重跑坐实」**：第一遍跑在 §五 那个轮转日志 bug 修好**之前**，读数是切片；
三格标了"不稳"的读数现在**逐格坐实**（下表右列），另外两格**被纠正**。

| 判据 | 第一遍（切片，不稳） | **重跑（整段，权威）** |
|---|---|---|
| 冲击读数 | `['yes']` | **`['no', 'yes']`** —— 第 13 个观测起为 `yes`，与 B 臂起点逐月对上 |
| **窗口读数** | `['active']` | **`['inactive', 'active']`** —— 与 `sp` 那局**同一个形状**（先关后开） |
| 策略牌读数 | `['ai_strategy_progressive_agenda']` | **`['ai_strategy_conservative_agenda', 'ai_strategy_progressive_agenda']`** —— 牌是**换过**的 |
| 立法读数 | `['law_landed_voting']` | **`['none', 'law_census_voting']`** —— 见下面的口径说明 |
| 改革派进过政府 | ❌（不稳） | ❌ **坐实**：整段 68 个观测里在朝**全是** `landowners` |
| clout 最高档 | `intelligentsia b18` / `industrialists b08` / `landowners b25`（下界） | **同样是 `b18` / `b08` / `b25`** —— 是全段水位，不再是下界 |
| **那条法换过没有** | ❌ `False`（下界） | ❌ `False` **坐实**：整局都没换 |
| 引擎侧判定 | ❌ 不通过（`suite_tests=2 / failures=1`） | ❌ 不通过（**一致**） |
| `error.log` 里我们的报错 | **0 条** | **0 条** |

> ⚠️ **立法读数两局不同，不许逐字比较**：第一遍读到 `law_landed_voting`、重跑读到
> `law_census_voting` —— 两条都在**投票权那一组**。这是**两局**（RNG 不同），
> 而且第一遍那个数是切片读出来的，所以只能说"两局推的都是投票权那一组里的某一条"。

### 2.1 与 `sp_empire_remnant` 摆在一起看，行为层出现了**跨档案差异**

两局的窗口形状一样（冲击落地那个月开），但**行为层不一样**：

| | `sp_empire_remnant`（西班牙） | `bv_alignment`（巴伐利亚） |
|---|---|---|
| 策略牌 | 全程只有 `conservative_agenda`（**没换**） | `conservative_agenda` → **`progressive_agenda`（换了）** |
| 在朝 IG | `landowners` ↔ `intelligentsia` **来回 23 次** | 全程 `landowners`（**没动**） |
| 立法 | `ENACT none` **67/67** | `none` → **`law_census_voting`**（推了） |
| 目标法 | 没换 ❌ | 没换 ❌ |
| 引擎判定 | 不通过 | 不通过 |

⇒ 「世界状态被改了、窗口开了」是两局共有的；**意图层动没动、推不推法，是逐档案不同的**。
这正是 G2「差分」想要的东西：**同一种"窗口开了"，落到行为层不是同一个结果**。
两局都没有换掉目标法 ⇒ 天花板（B53–B60）**没有被推翻**。

### 2.2 窗口开过这一条仍然值钱（与第一遍同一结论）

**它把两条从没在引擎里验证过的结构性判据，从「原版有先例」升级成「本机实测生效」**：

* `country_has_primary_culture = cu:south_german`
* `is_in_geographic_region = geographic_region_german_confederation`

这两条只写在 `je_sitai_bv_alignment_window` 的 `is_shown_when_inactive` 里。写错任何一个
字母、或者这两个触发器的作用域不对，**窗口就永远不开，而且完全静默**。

⇒ 批次 1–3 的结论「AI 依然不动手改法」应当细化为 **「动的手不是我们要它动的那条」**：
`bv` 推了 `law_census_voting`、`sp` 一条没推，而两局的目标土地法都没动。
归因（递牌通道 / 法律承诺 / `is_powerful` 没翻过线）仍属 B53–B60，**未解**。

## 三、`brz_market_loss`（2026-09-23，66 个游戏月，**整段读数**）

```powershell
.venv\Scripts\python.exe -X utf8 tools\probe\stage3_rerun.py --months 66 --archive brz_market_loss --fresh-logs
```

tick 走到 **1841.7.22.6**（≈66.7 个月）。盘上自报 **1,520 行、全部计入**（68 个观测块）。
这是第九份档案（失去市场 · 巴西）的**第一局**。

| 判据 | 读数 | 说明 |
|---|---|---|
| 冲击读数 | `['no', 'yes']` —— 第 13 个观测起为 `yes` | 与 B 臂起点逐月对上 |
| **窗口读数** | `['inactive', 'active']` | 与 sp / bv **同一个形状**：先关后开 |
| **意图层（牌）** | **不判** | 本档案**故意没声明** `reform_card`，见 §3.2 |
| 策略牌读数（原始，仍照实列） | `['ai_strategy_progressive_agenda', 'ai_strategy_conservative_agenda']` | **先进步、后保守** |
| 立法开过 | ❌ **`ENACT none` 68/68** | 一条候选法都没推 |
| 改革派进过政府 | ❌ 全程 `landowners` | |
| 各 IG 的 clout 水位（最高档） | `intelligentsia b18` / `industrialists b18` / `landowners b25` | |
| 本档案盯的法 | `law_tenant_farmers` | `[probe].reform_law`（B80） |
| **那条法换过没有** | ❌ `False` | |
| **引擎侧判定** | ❌ 不通过 | `suite_tests=2 / suite_failures=1`（挂的是 `law_changed`） |
| `error.log` 里我们的报错 | **0 条** | |

### 3.1 窗口开过：把这份新档案的判据从「原版有先例」变成「实测生效」

`je_sitai_brz_market_loss_window` 的门是 `c:BRZ ?= this`（平铺单国门）+ `has_variable` +
`legitimacy <= 75`。窗口开了 ⇒ 三条都成立。其中**平铺写法在巴西身上是第一次实测**：
原版确实有 `c:BRZ ?= this` 的先例，但那些在 `common/achievements/` 里、不是在 JE 里。
判据写错的表现同样是**完全静默** —— JE 就是不出现，引擎一个字都不报（与 bv 那两条结构性判据同一类）。

### 3.2 牌：**先进步、后保守** —— 而且这正好证明 B86 那条修法是必要的

巴西 1836 的初始政治牌里**本来就有** `ai_strategy_progressive_agenda`（B86 的由来），
所以分析器按新口径**不判**这一格，并把这句「不判」连同理由打了出来（实测输出：
`这一局盯的是 brz_market_loss，它没声明 [probe].reform_card —— 见该档案 [probe].why 里写的理由`）。

而**原始读数**说的是另一件事：这一局里牌**从进步换成了保守**。
如果沿用作废的旧口径（「进步牌出现过就算动过」），这一格会报一个**漂亮但虚假的「动过」** ——
而真实情况恰恰相反：冲击之后它退回了保守路线。**这就是 B86 那个陷阱的活实例。**

⚠️ 诚实项：这**不等于「档案没用」**，它说明的是**单看牌这一格不足以判「意图层动没动」**。
本局的读数组合是「窗口开了 ✅ · 改革派没入阁 ❌ · 一条法没推 ❌ · 牌退回保守」——
**世界状态确实被改了，意图层没有朝档案预测的方向动**。这与批次 1–3 记的天花板一致。

### 3.3 三局并列（跨档案差分）
| | `sp_empire_remnant`（西班牙） | `bv_alignment`（巴伐利亚） | `brz_market_loss`（巴西） |
|---|---|---|---|
| 窗口 | 先关后开 @13 | 先关后开 @13 | 先关后开 |
| 牌 | 全程保守（没换） | 保守 → **进步** | 进步 → **保守**（且按 B86 不判） |
| 在朝 IG | landowners ↔ intelligentsia 来回 23 次 | 全程 landowners | 全程 landowners |
| 立法 | `none` 67/67 | `none` → **推了投票权那一组** | `none` 68/68 |
| 目标法 | 没换 ❌ | 没换 ❌ | 没换 ❌ |
| 引擎判定 | 不通过 | 不通过 | 不通过 |

⇒ 三局共有的只有「**世界状态被改了、窗口开了**」；**意图层与立法层是逐档案、逐局不同的** ——
这正是 G2「差分」要看的分布。三局**都没有**换掉目标法 ⇒ 天花板（B53–B60）**没有被推翻**。

## 三·补、`tr_defeat`（2026-09-23，66 个游戏月，整段读数）

第九份之后补跑的一局（`--months 66 --archive tr_defeat --fresh-logs`），tick 到 **1841.7.3**、
自报 **1,355 行**（64 个观测块）。三件事：

| 判据 | 读数 |
|---|---|
| 冲击 / 窗口 | `['no','yes']` / **`['inactive','active']`** —— 与其它四局同一个形状 |
| 立法 | `['none', 'law_tenant_farmers']` ⇒ **✅ 真的在推那条土地法**（本系列第二次读到"AI 在推法"） |
| 改革派进过政府 | ✅（在朝只有 `intelligentsia`） |
| 各 IG 的 clout 水位 | `intelligentsia b25` / `landowners b25` / `industrialists b03` |
| **法律读数** | **`['law_serfdom']`** ⇒ 奥斯曼开局的土地法就是农奴制 ⇒ 已据此把它的 `[probe].reform_law` 由空填成实测值（九份**全部**声明齐了） |
| 策略牌读数 | ⚠️ **`[]`（一张都没读到）** —— 见下 |

### 三·补.1 两处判据侧的缺口（**都已修**）

1. **第四张政治议程牌没被记**（**已修，2026-09-23，backlog B87**）：探针的 `STRATEGY` 只记
   `progressive` / `conservative` / `reactionary` 三张，而白名单里有四张（第四张是
   `egalitarian_agenda`）⇒ 某一国挂着第四张时读数会是**空表**，而"空表"与"一张牌都没有"
   长得一模一样（B86 同族：**读不到 ≠ 没有**）。奥斯曼这一局的 `[]` 就是这么来的。
   查下去问题比原以为的更大：**原版政治槽有 10 张牌**，那条手写链漏掉的其实是 7 张
   （`great_reforms`、`maintain_mandate_of_heaven`、`meiji_restoration`、`nationalist_agenda`、
   `tanzimat_reforms`…）—— 而**奥斯曼在朝的那张恰恰就是 `tanzimat_reforms`**（`POLI` 行
   63 个月全是它）。
   **修法（已落地）**：候选集改为**从原版现读**（`type = political` 的全部牌）+ 补
   `ai_strategy_default` 与 `none` 两条兜底（链尾必有落点）；分析器把「一行都没有」与
   「牌是 none」**分开报** —— 上表那个 `[]` 现在会报成"读不到 `STRATEGY` 行"而不是"牌没挂上"。
2. **`error.log` 要读整组**（已修）：这一局的判定报了 `suite_our_errors = 9`，而前面四局都报 0 ——
   查下去是**同一个轮转盲区**（B85）换了个文件：`read_verdict` 只读 `logs/error.log` 那一份，
   而被轮转进 `error.1.log` 的行读不到 ⇒ "没看到"被当成了"没有"。现在按时间读整组
   （`rotated_logs` 一条规则管 debug 与 error 两族）。
   那 9 条本身是 **B74 已定口径的已知无害**（`Journal entry has redundant loc for …_goal`，
   每份档案一条）。现在它们被**分类**而不是被忽略：`SuiteVerdict.benign_errors` 照实报出来，
   但不参与 `ok` 的判定 —— 带我们命名空间的**其他**任何一行照样算失败。

### 三·补.2 **归因（B53–B60）**：这一局把"为什么法没换"钉到了**一条**通道上

有了逐月读数（`analyze` 的分类计数 + 把同一局的 `ENACT`/`LEG`/`GOV`/`POLI`/`INPUT`
按出现次序排成时间线），这一局的因果链是可以逐段读出来的：

| 环节 | 读数（按月份顺序压缩） | 判断 |
|---|---|---|
| 冲击 | `SHOCK` no×9 → **yes×55** | ✅ 落在第 10 个月，与阶梯对齐 |
| 改革侧输入 | `INPUT` no×33 → **yes×31** | ✅ 落下来了 |
| 窗口 | `JE` inactive×10 → **active×53** | ✅ 与冲击同月开，此后一直开 |
| **意图（牌）** | `POLI` **`tanzimat_reforms`×63（一次没换）** | ✅ 无阻塞：这张牌 `change_law_chance = 100`（`03_political_strategies.txt`）、亲 intelligentsia/industrialists/armed_forces、反 landowners、`pro_movements` 里就有 `movement_land_reform` |
| **目标法** | `ENACT` none×31 → **`law_tenant_farmers`×33** | ✅ **AI 自己在推那条土地法**（与 `INPUT` 同时开始，这正是"喂改革侧输入 ⇒ 意图落到那条法上"的直接读数） |
| **合法性** | `LEG` **b80×10 → b55×53** | ❌ **与冲击同月掉到最低档并一直没回来** |
| 政府在朝 | `GOV` intelligentsia×64 | ✅ 改革派一直在朝 |
| IG 实力 | `CLOUT` landowners/intelligentsia 都在 **b25**、industrialists b03 | ⚠️ 读数**分辨不出**我们那一档内的 −30%（见下） |
| **法律** | `LAW` **law_serfdom×63** | ❌ 33 个月没推成 |

⇒ **结论（本局）：阻塞不在"想不想"（牌 100 的权重 + 改革派在朝 + 目标法正确 + 真的推了 33 个月），
而在立法的"过不过"那一侧 —— 合法性整段落在我读到的**最低档**（≤55）**，
而原版 `laws/readme.md:9-10` 点名的两条让法推不动的理由里，"缺政府支持"正是这一条。**

⇒ ⚠️ **机制更正（2026-09-24，见 `阶段5-B88-杠杆设计.md`）**：上面那句「阻塞在合法性那一侧」**方向对、机制错** —— 合法性不在停滞公式里。停滞由 `LAW_ENACTMENT_IG_CLOUT_STALL_EFFECT`（`00_defines.txt:177`）乘**反对派 IG 的心情系数**（中立 0.5 / 不满 1.0 / **愤怒 1.5**，`:178-180`）与 `LAW_ENACTMENT_MOVEMENT_SUPPORT_*`（`:175-176`）决定；而「连续 33 个月都在 `is_enacting_law`」本身就说明它在**停滞**（真失败会结束立法并进 730 天冷却，`:171`）。
⇒ **设计含义（记进 backlog B88）**：档案的 `[probe]/reform_inputs`（+50% 智力界/工业家政治力量）
喂的是**牌的权重输入**，而这一局证明那一侧**本来就不是瓶颈**；真正没被触及的是
**合法性 / 立法成功率那条通道**。正式版要动的是"让改革派的实力强到能组出高合法性政府"
（即压 landowners 的**制度底座**，而不是再给改革派加成），否则每一局都会停在同一处。

> ✅ **这条边界已经拆掉**（2026-09-24，backlog B90）：探针改成记**数值** ——
> `LEGV`/`LEGF`（合法性）、`CLOUTV`/`CLOUTP`（三个相关 IG 的政治力量）、`PROG`（立法进度）。
> 依据是三条反证（原版提示逐字用 `[GetPlayer.GetGovernmentLegitimacy|v]`；`debug_log` 会展开 data function ——
> 原版 `00_code_on_actions.txt:854` 自己就在用同一条作用域链；标识符在 exe 里）。
> **下一局就能直接回答 B88**：压力前后合法性动了几分、IG 政治力量各是多少、立法进度推到哪里。
>
> ⚠️ **一条测量边界**（这一局暴露、**已按 B90 解决**）：`CLOUT` 是**单调夹逼读数**
> （`≥ b03/b08/b12/b18/b25`），所以"某一档**之内**的 −30% 政治力量"**根本量不出来** ——
> 上表 landowners 与 intelligentsia 都是 b25，看起来"我们的压力没生效"，
> 而真实差别可能就在这一档里。这与 `stage3_rerun` 里那条教训同源
> （**单调夹逼读数只能取极值**）。要量档内变化得换读数手段（见 B88 的第 2 条）。

## 四、`eg_debt`（2026-09-23 **重跑 66 个月**，整段读数）

批次 2 的 `阶段5-批次2-结果.md` §三写着「`eg_debt` 没跑 A/B 会话」，之后补跑过一局 27 个月，
但读数一直没写进任何文档，而且那 27 个月那一局用的是**修复前**的分析器（见 §五）。
本轮按与其它档案同一口径重跑：

```powershell
.venv\Scripts\python.exe -X utf8 tools\probe\stage3_rerun.py --months 66 --archive eg_debt --fresh-logs
```

tick 走到 **1841.8.29**（≈67.9 个月）。盘上自报 **1,448 行、全部计入**（69 个观测块）。

| 判据 | 27 个月那一局（旧分析器） | **66 个月（整段，权威）** |
|---|---|---|
| 冲击读数 | ✅ | `['no', 'yes']` |
| 窗口读数 | ✅ 开过 | `['inactive', 'active']` —— 与其它三局**同一个形状** |
| 各 IG 的 clout 水位 | `intelligentsia b18` / `landowners b25`（下界） | **`intelligentsia b25` / `landowners b25`** —— 智力界真到了 b25 |
| 策略牌读数 | （没记） | `['conservative_agenda', 'progressive_agenda']` ⇒ **✅ 那张牌挂上过** |
| 改革派进过政府 | ❌（下界） | ❌ **坐实**：全程 `landowners` |
| 立法开过 | ❌ `ENACT none` 27/27 | ❌ **`ENACT none` 69/69** |
| `error.log` 里我们的报错 | — | **0 条** |

### 4.1 这一局是**全系列第一次拿到「引擎判定：通过」**

`suite_tests = 1`、`suite_failures = 0` ⇒ `引擎判定：通过 ✅`。

⚠️ **别把它读成"这份档案成功了"**：它的套件**只有一条判据**（`reform_window_opens`），
因为 `eg_debt` 当时**没有声明** `[probe].reform_law` ⇒ 「法律换过没有」那条判据**不产出**（B80 的口径：
没查实的法不判）。所以这个"通过"说的是**窗口开过**，不是"改革发生了"。
其余三局是 2 条判据的套件（窗口 + 法律），它们都挂在 `law_changed` 上。

### 4.2 顺手把那条"没查实"补上了（有读数了）

那一局的月度 `LAW` 读数**全程只有 `law_serfdom` 一种**（69 个观测块，去重后就是 `['law_serfdom']`）
—— 也就是说**引擎默认给埃及的土地法就是农奴制那一条**。`LAW` 读的是土地改革那一组的现行法
（另两局分别在西班牙读出 `law_latifundias`、在巴伐利亚读出 `law_tenant_farmers`，与各自声明的
`reform_law` **逐字一致** ⇒ 这条读数可信）。据此把 `eg_debt.toml` 的
`[probe].reform_law` 由空改为 `"law_serfdom"`，并把"先留空、后来实测查实"的过程写进它的 `why`。

⚠️ **奥斯曼（`tr_defeat`）仍然留空**：它没有过这样一局实测，**不许照抄埃及这一条** ——
B80 的教训就是"照抄一条没在本处境实测过的法，会让判据当场为真、报出假的「法律换了」"。
它要补，得先跑一局。

## 五、这一页带出来的工具改动（含一个**真被读错过的 bug**）

| 改动 | 为什么 |
|---|---|
| `pdx.game_auto` 的后两步 + `SuiteVerdict` | 闭环原来只到"切回后台"，判定与读产物是手动的；现在 `run --wait-tests N` 一条命令做完，**不通过退 1** |
| `stage3_rerun.engine_verdict()` | 探针原来把成绩单**直接挪走**（为了不让 `v3 verify` 红），从来不读它 —— 现在读数发生在挪走**之前**，并把判定摘要写进报告 |
| `stage3_rerun._subject_archive()` 的 tag 反查 | `TARGET` 只在"武装"那一刻写**一次**，落在**哪一份**轮转日志里取决于那一局写了多少 ⇒ 只看 `debug.log` 会误判成"这一行没落"。改成：有 `TARGET` 用它，没有就按 `ROLE;<TAG>` → `[archive].country` 反查（档案↔国家是数据源里的事实，P9）；一个国家对上多份档案时**不猜** |
| **`stage3_rerun.debug_logs()`：轮转日志按时间排序** | **本次修掉的真 bug**，见下 |

### 5.1 轮转日志的顺序错了一次，读数因此是**不连续的切片**

`debug.log` 到 512 KB 就轮转成 `debug.1.log` …（本次一局产生 4 份）。
分析器原本用 `sorted(base.glob("debug*.log"))` —— **字典序**是
`[debug.1, debug.2, debug.3, debug.4, debug.log]`：**当前那份排最后、次新的排最前**。

后果不是"少读一点"，而是**读出一段不连续的切片**：拼接后的文本里找"最后一次武装"（`RUN` 行）
会切在最旧那份的中间，于是 `debug.3` / `debug.2` / `debug.1` **整份被丢掉**、
最新的 `debug.log` 被接在后面。实测（`sp` 那一局）：**盘上 1,401 行自报，分析器只报了 402 行**，
并且报出一个**假的**「窗口 `inactive → active`」（把最旧那份的尾巴接到最新那份上拼出来的）。

**两处修法**：
1. `debug_logs()` 按**时间**排序：编号大的在前（`debug.4` → `debug.1`），当前那份最后；
2. `only_after_arm` 那把 `RUN` 剪刀**在有 `--fresh-logs` 时不再剪**。它的原意是排除
   **上一局**留在轮转文件里的尾巴；但观察者局里没有 `RUN;A`（决议点不到），
   第一次武装标记是第 37 个月的 `RUN;B` —— 于是它会**把这一局自己的前 37 个月剪掉**，
   把"窗口开过没有"这类**存在性**判据悄悄变成"第 37 个月之后开过没有"。
   现在读数会同时报**盘上自报行数**与**算进读数的行数**，被剪掉多少一眼可见。

⚠️ **已受影响的读数**：`bv_alignment` 与 `eg_debt` 那两局（§二、§四）都是旧分析器出的。
`sp_empire_remnant` 已按新口径重算（`--analyze-only --whole-log`，**不重开游戏**）；
`bv_alignment` 已**重跑一局**（§二 的表：三格坐实）。

### 5.2 同一轮里我还犯过一次**同源**的错，一并记下来

修好顺序之后回看，**上一轮把「`TARGET` 自臂行没落」当成结论，本身就是同一个轮转盲区**：
当时是拿 `Select-String` 去 grep **`debug.log` 那一份**，得到 0 条就写了"它没落"。
重跑那一局的实测是：`TARGET` **在 `debug.1.log` 里（1 条）**、`debug.log` 里 **0 条**。

⇒ **教训（比那个 bug 本身更值得记）**：凡是"日志里有没有 X"的判断，
**口径必须是"整个日志集合"**，不是"当前那一份"；只看 `debug.log` 得到的"没有"
**从来不是证据**（它与"没发生"长得一模一样）。修法上也照此办理：`_subject_archive()`
的 tag 反查保留（它让认主语不再依赖任何一条只在某一刻写的日志），但**理由改成了正确的那个**
（`tools/probe/stage3_rerun.py` 的 docstring 已同步改）。

## 六、还欠的

| 欠项 | 状态 |
|---|---|
| ~~`bv_alignment` 的整段读数~~ | ✅ 已重跑坐实（§二） |
| ~~`eg_debt` 的长局复测~~ | ✅ 已重跑 66 个月（§四），并顺手把 `reform_law` 由"没查实"补成实测值 |
| ~~`tr_defeat` 的 `[probe].reform_law`~~ | ✅ 已实测填上（§三·补：奥斯曼开局的土地法就是 `law_serfdom`，九份全部声明齐了） |
| G-EXIT-5.4（G4 不退化） | ⏳ 需要**试玩者**（只有人能关） |
| ~~B53–B60 那一族（为什么推的不是目标法）~~ | ✅ **归因已落地到一条通道**（§三·补.2）：`tr_defeat` 那一局牌没换、目标法正确、**AI 自己推了 33 个月**，而合法性从冲击那一月起落进最低档并一直没回来 ⇒ 阻塞在**立法的"过不过"那一侧**，不在意图侧。设计侧跟进记在 **B88**（现有 `reform_inputs` 喂的是牌的权重输入，而那一侧本来就不是瓶颈） |
