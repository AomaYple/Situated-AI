# backlog · 悬而未决的问题（不是否决，是排队）

> **用法**：任何"发现了但挡不住当前阶段"的问题，30 秒落一条，不展开讨论。
> 每条格式：**问题 → 我的推荐值 → 何时才需要（触发条件）**。
> 触发条件到了才把它拉进执行文档；否则一直躺在这里。

---

## 一、挡阶段 0（方向定稿）——需要你拍板

| # | 问题 | 我的推荐 | 何时需要 |
|---|---|---|---|
| B1 | 五条契约（摩擦粒度 / 目标函数记账 / 三行解释 / 性能预算 / 难度三档） | 按 `01a` J 节的建议值执行，后续可改 | **现在**（阶段 0 收尾） |
| B2 | 第一版处境档案选题与范围 | 「战败求存」×**俄罗斯单国**；奥斯曼留作第二个档案 | 阶段 3 开工前 |
| B3 | 命名空间与目录布局 | `sitai_` 前缀 + 仓库根 `mod/`（数据源 `mod/data/`、生成器 `tools/pdx/`） | 阶段 1 出第一份生成物前 |
| B4 | 设计文档与生成器是否入库推送 GitHub | 入库并推送，每阶段一个小提交 | 阶段 0 收尾 |
| B5 | 游戏内验证方式 | 复用探针流水线（`v3 experiment`）+ 一个"同时启用真 mod"的小扩展 | 阶段 2 前 |
| B6 | 发布节奏 | 每阶段给一个可安装、可回滚版本 | 阶段 2 结束时 |

## 二、挡阶段 1–2（可执行面 / H1）

> 阶段 2 已于 2026-09-20 收口（`exec/阶段2-结果.md`）：B7/B8/B9/B10 与 H3 已结案，
> 结论直接抄在这里，不再占位。

| # | 问题 | 结论 / 我的推荐 | 状态 |
|---|---|---|---|
| B7 | 是否真能加第 4 个槽 | **不行**：三局探针共 0 次（`type = sitai_fourth`、权重 1000 也从没被抽中）→ 槽位固定三个 | ✅ 结案 |
| B8 | 三槽是否独立？能否同时占两/三个槽 | **能同时占满三个**：风暴局 25.2% 的观测三个槽都是我们的牌（自然局 0.3%）。各槽独立持有、独立重抽（节奏不同：34%/57%/51%） | ✅ 结案 |
| B9 | 引擎重抽多频繁 | 自然：政治 1.05% / 行政 **0.07%** / 外交 3.26%（每月每国）；`NAI` 周通道 ≈9.6 年一次。**节奏可用小文件覆盖接管**（→ B26） | ✅ 结案 |
| B10 | 新增策略是否必须带 loc / icon | **不必须**：无 loc/icon 的牌被抽中 16+17 次、零报错。但仍一律带（可读性 + 官方口径） | ✅ 结案 |
| B11 | 我们的牌在 `secret_goal_*` / `wargoal_*` 上写的权重，原版外交决策是否真读 | 阶段 3 顺带验证（决定"逐目标通道"带宽） | 待验 |
| B12 | 不同 `type` 的策略是否会读跨域字段 | 不依赖；只在对应域写字段 | 待验 |
| B13 | `ai_strategies` 是否支持数据功能前缀（`REPLACE:`） | 仅当必须精确改某个原版策略时才需要（R2） | 阶段 4 之前不必 |
| **B26** | **节奏杠杆的档位**：`CHANGE_STRATEGY_THRESHOLD` / 周概率调到多少才是"活"而不是"反复无常" | 推荐从温和档起步（门槛 ≈40、周概率 ≈30），**用行为质量（G4）而不是换牌率来定档**；风暴档（门槛 1）只是实验设置 | 阶段 4 |
| **B27** | 节奏杠杆的**性能开销** | 风暴局（每周重抽）的引擎成本未测 → 进阶段 4 的压力场景（`gametimer` 均值 + 最坏帧） | 阶段 4 |
| **B28** | 整套并行跑偶发红（worker `INTERNALERROR`、`test_cli` 快照用例等负载敏感用例假红） | **已定位并处理一半**：根因是"覆盖率 + `-n auto`"——本机 16 核下每个 worker 各写 coverage、各重跑 16 秒的全量分析。实测：`-n auto` 376 秒且偶发红；`-n 4` **185 秒全绿**。已把 `v3 cov` 固定成 `-n 4`（覆盖率门禁 376→**173** 秒）。**仍待办**：`addopts` 的默认 `-n auto` 要不要跟着调小（它的实测口径是 `--no-cov`，两者不矛盾） | 阶段 3 顺手 |

## 三、挡阶段 3–5（闭环 / 框架化 / 扩张）

| # | 问题 | 我的推荐 | 何时需要 |
|---|---|---|---|
| B14 | 账本因子的最终清单（第一版 8–12 个，哪些直读、哪些代理） | **阶段 1 已给依据**（`exec/阶段1-结果.md` §三）：取命中的 11 个因子，两个 ❌ 改走 A 级 | 阶段 3 |
| B15 | "改革阻力"等合成量的代理公式（在哪会失效） | 先用 IG + 法律 + 统治者意识形态三项合成，标注为近似 | 阶段 3 |
| B16 | 累积量的衰减曲线（战败记忆多久消退） | 先线性衰减 60 个月，之后按实测调 | 阶段 3 |
| B17 | 三行解释用什么载体（JE？scripted GUI？国家面板？） | 先用 JE + tooltip（最省、可卸载）；GUI 留到阶段 6 | 阶段 3 |
| B18 | 难度三档的具体差异（对玩家宽/严到什么程度） | 先做"一视同仁"默认档，另两档留空壳 | 阶段 6 |
| B19 | 是否需要"目标函数表"的正式数据格式 | 阶段 4 schema 定稿时一起定 | 阶段 4 |
| B20 | 压力场景的标准剧本（大战 + 破产 + 革命潮）怎么构造 | 用固定存档 + 固定指令序列，做成可重放脚本 | 阶段 4 |
| B21 | 档案数量上限（10–20 里到底几个） | 先不设上限，按"每个都能挂判据"筛 | 阶段 5 |
| B24 | **字段输入远多于权重输入**（`unify_germany`：权重 11 / 字段 41） | 阶段 1 实测结论：可喂输入**以牌面字段为主**，方案按此重排（权重只做"选哪张牌"，字段才决定"拿到牌后怎么打"） | 阶段 3 |
| B25 | 「合法性」「战争支持」在策略侧**不读** | 只能走 A 级（真实改变世界）；若阶段 3 需要它们作为**读入**，需另找代理量 | 阶段 3 |
| **B29** | **「谁算战败」挂哪个挂载点** | 原版 `on_war_end` 只给 `scope:actor`/`scope:target`、**不带胜负**（00_code_on_actions.txt:7043），硬挂会把战胜方也记成战败。当前档案刻意不绑 on_action，冲击做成脚本效果 `sitai_ru_defeat_shock` 由 A/B 实验施加；**判定规则等实验结论**（可考虑：割地/赔款类和平条款 + 首都被占） | 阶段 3 实验后 |
| **B30** | **节奏杠杆的全局作用域**（defines 没有单国写法） | 现状：`NAI` 两个键一改，**所有国家**的重抽节奏都变 —— 这是有意的（目标本就是让每个国家都能重新推导），但意味着它不能用来解释单国差分，也不能在难度档里"只对玩家生效"。若将来需要单国/分档控制，出路只有：把重抽做成 JE/事件驱动（A 级），或按国家类型分槽给不同权重 | 阶段 4–6 |
| **B35** | **实验用直接修正 → 正式档案的真实触发 + 因果链** | 阶段 3 施加的两处都是 `add_modifier` **直接改数**（`sitai_ru_defeat_pressure` 三条 + `sitai_ru_reform_inputs` 两条），且刻意不绑任何 on_action。正式版必须换成：真实触发（输掉战争 / 割地赔款类和平条款 + 首都被占，与 B29 同一个判定）→ 因果链（战时消耗 → 激进派与合法性自然下滑；赔款 → 真实财政动作；工业化/识字率位移 → IG 势力自然变化）。阶段 3 的数值作为**实验设置**留在数据源里，并在 `why` 里逐条标了"实测调档"；正式档案另起一份，不改这份实验档案 | **阶段 4 档案定稿前** |
| **B36** | **改革侧输入的档位够不够翻过 `is_powerful`** | B2 段取原版强档 **+0.5**（`shogun_ig_forced_to_open_market`，content_1_modifiers.txt:1462-1470；原版档位 ±0.03/0.05/0.10/0.15/0.25/0.5/0.75），但 `is_powerful` 是**相对份额**判定，抬一档未必翻得过去。**做法：先用探针量 IG 势力实际水位再决定档位，不许盲调数字**（判据已预注册在 `exec/阶段3-归档清点.md` 第四节） | **下一局跑完**（B2 段 ≥12 个月观测时） |
| **B37** | **`v3 ab --health` 在观察者局的假红** | 与 B33 同源（观察者模式没有玩家国家 → 探针 `PLAYER` 行 0 条），但落在**分析器口径**上：现在第②条"玩家不是主角国家"会判红，而那种局面下主角国家**必然**是 AI —— 判红是假红。推荐：把"一条 PLAYER 行都没有"判为通过并在明细里写明理由，把"月度钩子没跑"交给第③条"观测在流动"兜（那才是这条失败的直接证据）；同时保留"用旁观者小国跑"这条已验证可用的路 | **下一局之前**（下一局就是观察者点火） |
| **B38** | **`scripted_tests` 的收敛覆盖要随游戏版本核对** | 引擎会跑 `tools/scripted_tests/` 下的**每一份**套件（mod 提供的也读，阶段 3 已实测），所以探针把原版那几套压成"立即收敛"，否则 `-scripted_tests` 一局被拖到 1870–1900 年。原版**新增**套件时这份清单会漏。推荐：清单只留在 `ab_probe.VANILLA_SUITES`（一处定义），升级后核对一次 | **游戏版本升级后**（与闸门①/②同时） |

## 三·五、自动化范式（2026-09-20 探索，详见 `exec/自动化范式.md`）

> 结论已落地：**推荐范式 = D 点火（UI 自动化）+ A 跑与判定（官方 `-scripted_tests`）+ E 观测（日志真值）**，
> 已实现为 `tools/pdx/game_auto.py`。下面只是这条路**还没走完**的部分，不是"要不要做"。

| # | 问题 | 我的推荐 | 何时需要 |
|---|---|---|---|
| **B31** | **合成的键盘输入：虚拟键无效，但扫描码有效**（2026-09-21 更正）。老结论"引擎不消费注入的键盘事件"只对 `keybd_event` / `SendInput` 的**虚拟键**路径成立；换成 `pydirectinput`（源码里带 `KEYEVENTF_SCANCODE`）之后**空格被接受**：实机 `start_background_session()` 里按空格，`Processing Tick` 从 1836.1.2 走到 1836.1.12.18，且**前台不是游戏**。**控制台**那 6+4 个候选键仍**未证实**（用"命令会写的文件有没有变"当判据，4 个键都没写文件；该探针 2026-09-22 已删）—— 但那一次分不清"键没被接受"与"这个界面不让开控制台" | 控制台路线**不再判死**，只是未证实。要继续试就换两个变量：① 在**跑起来的局**里试（不在大厅/菜单）；② 候选键补齐（`pydirectinput` 用的是扫描码，键名表与虚拟键不同）。另一条更省事的路子已经可用：`-run_console_action="<命令>"`（见 §3.2 与 `01a` P.2），一次性命令根本不用开控制台 | 只有在"必须在局内反复执行控制台命令"时才需要 |
| **B32** | 存档能不能当**固定开局**（"零点击开跑"的唯一出路）。**2026-09-21 补两条实测**：① 官方 `scripted_tests` 套件格式里**没有**存/读档指令（`germany/italy/springtime/ip3/test.txt` 全查过，只有 `last_date` 与 `tests = { success/fail }`）⇒ **套件路线排除**；② `continuelastsave` / `loadsave` 两个字面量在 exe 里属于 **`frontendidlerlogic.cpp` 的前端 idler 消息**（上下文含 "Quit: Idler message" / "jomini_message" / "Could not load save game [{}]. Going to main menu"）⇒ 它们是**启动器点「继续」时经进程间通道发给游戏的消息**，不是命令行开关（`-load*` 依旧 0 命中）。另注：`continue_game.json` 实测**会被游戏写**（本机内容是 `{"title":"autosave","desc":"正在观察NONE",…}`，说明上一局确实进了观察者模式），但"直起 exe 时它会不会被自动读"**仍未实测** | **两条候选**：① 直起 exe 后看主菜单是否直接载入 `continue_game.json` 指向的那一局（一次启动就能判）；② 复刻启动器的 idler 通道（管道名 + 报文格式）—— **启动器本机常驻**（`ps_service_launcher` / steam 进程在跑），所以"驱动启动器"比"逆协议"更现实。判成功 = 启动后**不点任何东西**，`dedicated_server.log` 的 tick 从存档日期继续走 | **阶段 5 的固定开局**；也是"后台跑批"的最后一块 |
| **B33** | **观察者模式没有玩家国家**：探针里 `is_player = yes` 那段在观察者局里 0 次触发（`ZZPROBE AB;PLAYER` 0 行），但 `on_monthly_pulse_country` 照常对每国触发 | A/B 档案**不要**依赖"玩家是谁"这个量。两条出路：① 判据换成不依赖 `is_player` 的量（探针里的 `ROLE`/`SHOCK`/`LEG`/`JE`/`LAW` 那几条本来就不依赖）；② 开局方式改成 lobby 里「随机国家 → 开始」两次点击，拿一个真玩家国家 | **阶段 3 实验设计定稿前**（属于"不改就白跑一局"的那种） |
| **B34** | 按钮模板与**界面语言 + 分辨率 + `GUI.scale`** 绑死（模板是本机 1920x1080 简体中文下裁的） | 当前靠**多尺度匹配 + 找不到就报错**顶住。若将来要在别的分辨率/语言上跑，就按分辨率收多套模板目录，而不是把坐标写死 | 换机器 / 换语言 / 游戏改 UI 时 |
| **B39** | **本地 mod 和一堆 Workshop 条目混在一起时，本地 mod 不被引擎挂载**（阶段 4 已定位到组合，未定位到机制）：`content_load.json` = 23 条 Workshop + 1 条本地（`…\mod\sitai_ru_defeat`）时，我们的 mod 在整个 `logs/`（含 `debug.1–5` / `error.1–5`）**0 命中**、连 `Mounted Data` 都没有；把**阶段 3 确实挂载过的**探针 mod 一起放进去 —— **它同样没挂上**，而同一局里 Workshop mod 的本地化文件照常被解析（`ULTRA_CD_cultures_l_simp_chinese.yml` 等）。**只放本地 mod（2 条、`enabledUGC` 空）时一切正常**：`Mounted Data: …/mod/sitai_ru_defeat` + 引擎按合成名列出该 mod。**已排除**：多档案合成元数据（把安装副本的 `id` 改成 `""`、描述截到 200 字，仍不挂）、JSON 写法（改成启动器那种紧凑 `separators=(",",":")` 写法，仍不挂） | 不要再猜 mod 内容。下一步只查两件事：① 本地 mod 是否还需要**启动器侧注册**（`launcher-v2.sqlite` / playset）；② `enabledMods` 与 `enabledUGC` 的分工。判据 = 混着 23 条 Workshop 时 `Mounted Data` 里能看到本地 mod。**在那之前，"要看引擎怎么说"就跑"只启用本地 mod"那一局** —— 阶段 4 的 G-EXIT-1 实机证据正是这么拿到的 | 阶段 5 起任何"要看引擎怎么说"的判据 |
| **B47** | **G-EXIT-3 对照表：探针 mod 装得上但"没挂载"**（2026-09-21 夜实测）：把带 dump 按钮的本地 mod 装进 `…\mod\zz_sitai_perf`、`content_load.json` 只留它一个，起游戏进局后**颜色按钮 60 秒没出现**，且 `logs\debug.log`/`system.log`/`error.log` 里**没有 `Mounted Data`** ⇒ 本地 mod 没挂上（B39 那个家族：本地 mod 的挂载条件）。**已入库的工具**：`tools/probe/perf_mod.py`（生成探针 mod：往原版 `gui/error_deer.gui` 的 flowcontainer **插**两个按钮，品红=CLEAR、青=DUMP）+ `tools/probe/perf_compare.py`（两局对照驱动：清场 → 后台启动 → 闭环进观察者局 → 按**颜色质心**点按钮 → 跑 N 月 → dump → 解析；`finally` 还原 `content_load.json` 并删本地 mod，用户 23 条 Workshop 配置一字不动）。实测已跑通的半程：进局 `borrows=0 / borrow_seconds=0.0 / speed_ok=True / rate 3.0–3.3 天/秒`、稳定跑满 **12 个月（375.7 天）** | **下一步（按最佳方案）**：run A 用"**只带 gui 的壳 mod**" —— 复制我们 mod 的 `.metadata/metadata.json`（那个 id 启动器认过、实测挂载成功）+ 探针 gui，不带 gameplay 内容；run B 用完整 mod + 同一份 gui 覆盖。这样两局**挂载机制一致**，差异只剩我们的内容，B39 也不再干扰。**在此之前不要重跑两局**（每次烧 ~6 分钟实机）：先确认"进局时打印的挂载证据非空" | 阶段 4 出口（G-EXIT-3）|| **B40** | **F9 的「单帧 ≤0.5ms」用现有仪表量不出来**：`gametimer` 最细只到 `Day`（12 份真实 tsv 三列 `Game Date / Time Unit / Seconds`，`Time Unit` 实测 `Year 8 / Month 99 / Day 13019`，全文 `Frame` **0 命中**） | 把预算口径改成**可测**的那一条：每游戏日墙钟均值 + 最坏一天（现成读数：Day 均值 0.0918s、最坏 1.7063s）；或在冻结文档里明写"该条用现有仪表无法核验"。**不要**拿日总量除一个假设帧数凑出"单帧耗时" | 阶段 4 出口验收 / 阶段 5 性能回归之前（口径不改，验收表永远缺一格） |
| **B41** | ~~`v3 modguard` 与 `v3 ai-surface --check` 没有离线通道~~ **✅ 已做（`d3657ce`）**：新增 `pdx/vanilla_index.py`（原版真值的统一读取口，快照优先/回落游戏）+ 快照 6 个新域（`vanilla_keys`/`vocabulary`/`modifier_fields`/`mod_paths`/`icon_paths`/`ai_surface`，5.08 → 6.03 MiB，**旧 7 个域逐域 unchanged**）+ `--offline` 开关，两条命令在 `V3_ROOT` 指向不存在路径时 **exit 0** 且结论同源（实测「五道闸门全过」/「一致」），并已进 CI（check job 现 12 步） | **剩余**：`ai-surface --write` 仍只走在线口径；离线池目前只覆盖闸门 ①②（③④⑤ 本来就不读游戏）。若将来再加快照域，判据不变：**缺域必须报未覆盖 + exit 2**，不许静默通过（`vanilla_index` 的 `None` = 未覆盖，不是空集） | 已完成 |
| **B43** | **覆盖率门禁差 0.1 个点：`pdx/modguard.py` 95.9% < 96%**（阶段 4 收口的最后一条红）。`v3 cov` 实测「整体 **86.94%**（下限 86%）、低于下限 **1 个模块**」。**已做**：新增 `tools/tests/test_modguard_offline.py`（11 条，快照源正路 / 缺整个域 / 域内缺目录逐条列未覆盖 / `None`≠空集 / 两源集合一致 / 薄包装），把 `modguard.py` 从 **88.0% 拉到 95.9%**。**还差的行**（`pytest --cov=pdx.modguard --cov-report=term-missing` 实测）：`496, 505, 521, 530, 533, 542`（闸门 ② 各池"缺目录"的 `uncovered.append`——注意它在缺整域时会走 `_vanilla_missing` 早退，所以要用"域在、项缺"的**部分**快照去够）、`720-721`（元数据反解抛错）、`759`（两次生成不一致）、`844-845`（`format_report` 打"没过且有明细"的闸门） | **不许下调 `covgate.FLOORS`（P6 只许上调）**。差一个点就到：优先补 `530/533`（图标池缺目录：让 `ctx.root/icon` 不存在且 `icon_exists` 返回 `None`）与 `844-845`（造一个"没过且带 details"的闸门再 `format_report`）；`496/505/521/542` 要用**部分**快照（域在、目录缺）而不是整域缺失 | ✅ **已清（2026-09-21 夜）**：新增 `tools/tests/test_modguard_branches.py`（10 条定点分支用例，踩到 496/505/521/530/533/535/720-721/759/844-845 这些"**域在、项缺**"形状的分支）；`v3 cov` 实测整体 **88.02%**、**低于下限 0 个模块**、**exit 0**。⚠️ 如实记一条：`modguard.py:542`（defines 参数池未覆盖）在**离线路径上不可达** —— "域在但空"会被 `missing_sections()` 判成缺域、先走前置条件早退；用例改为**钉住真实行为**（走前置条件），既不硬凑覆盖率、也不下调 `FLOORS` |
| **B42** | **`v3 cov` 的退出码与它自己的判据不一致**：在冻结树（`f31d87b`、工作树干净）上，`v3 cov` 打出「整体 **87.96%**、低于下限 **0 个模块**」**却没有 exit 0**（退出码 1）；同一份 `cov.json`（时间戳 `2026-09-21T01:22:37`）跑 `v3 cov --check-only` 是 **exit 0**；而直接跑 `pytest -n 4 -q -p no:randomly` **两次都是 1173 passed / 10 skipped / 0 failed**（160.2s / 163.4s）。**已排除一半**：把 `covgate.run_pytest()` 的命令**逐字复现**（`python -m pytest -q -n 4 --cov-report=json:… --cov`，不带 `-p no:randomly`）→ `1173 passed / 10 skipped / 0 failed`、`Total coverage: 87.63%`、退出码正常；而 CLI 的退出逻辑只有两处会退 1（`code != 0` 只**打印**黄字不退出；真正的 `raise typer.Exit(EXIT_FAILED)` 只在 `failed or below_overall` 时）⇒ 所以那两次多半是 **cov 内部那次 pytest 偶发非 0**（B28 家族的 xdist 抖动），不是判据错。**注意**：B43 那次 cov 的 exit 1 是**真红**（`modguard.py` 低于下限），与这条不是一回事 —— 定性时要分清 | **2026-09-23 又观测到一次**：`v3 cov` 打出「整体 **88.61%**、低于下限 **0 个模块**」却 exit 1；**紧接着原样重跑一次是 exit 0**（`--check-only` 也是 exit 0）。⇒ **偶发确认，但两次都没留下可查的证据**（`run_pytest()` 用继承 stdio 跑，输出过了就没了）。**下一步（未做）**：给内层命令加 `--junitxml=tools/out/cov-junit.xml`，非 0 时把失败用例逐条打出来 —— 那才是能定性的证据。**注意**：B43 那次 cov 的 exit 1 是**真红**，与这条不是一回事 | 阶段 5 起每次"全量门禁"都会碰到 |
| **B44** | **游戏安装目录被塞进 2 个非游戏文件 ⇒ 13 条门禁同时红**（2026-09-21 实测）：`gfx\portraits\accessory_variations\fee87a16-….dmp`（149,078 字节，崩溃转储）与 `gfx\cursors\.XnViewSort`（2,854 字节，XnView 的排序缓存，**打开一次图片目录就会生成**）。两者让 `game` 全树从 **27,723 → 27,725** 个文件，于是 `v3 verify` 的 5 条 claims（`env.game_files_doc01` / `tree.game` / `tree.game_bytes` / `tree.gfx_files` / `tree.gfx_bytes`）全红，连带 `test_verify` / `test_cli` / `test_doc_tables` / `test_doc06` / `test_install_hygiene` / `test_golden` / `test_markers` / `test_cli_more` 共 13 条 | **已做**：按 `test_install_hygiene` 自己的口径把两个文件**移出**（不是删）到 `%TEMP%\v3_quarantine_20260921\`，复查 `game` 全树 = 27,723，上述 147 条用例全绿。**教训**：门禁一红就去看**判据行**指向哪条 claim，别急着当成自己刚改的代码 —— 这次 13 条红与当次改动**毫无关系** | 再遇到"一片红但改动很小"时 |
| **B45** | **后台点击的成熟库候选路线还没实机验过**（2026-09-21）：手写 ctypes 的三条消息注入（`PostMessage` / `SendMessage` / `+WM_ACTIVATE`）已实测死（按钮分数 0.855 一动不动）；换成成熟库后**能力面**复核确认没有任何库提供"向非前台窗口投递原始输入"的入口（`SendMessage/PostMessage` 即同一条死路），但多出一条**从没测过**、且**纯成熟库可写**的候选：`win32process.AttachThreadInput`（pywin32 有）+ `win32gui.SetFocus` + 真实输入（`pydirectinput`），**全程不调** `SetForegroundWindow` | 探针（2026-09-22 已删，结论留档）判据 = **逐 tick 日志**（点中「观察」⇒ 观察者局开始走 ⇒ 日志出现 tick；大厅是暂停的、本来不写日志），并带**阳性对照**（真借前台点一次必须出现 tick），否则本次实验作废。**不许用像素判分**（`ImageGrab` 只能抓前台窗口）。跑完按结果更新 `自动化范式.md` §4.2 / §8 | ✅ **已实测（2026-09-21，两次复现）**：这条路**可行** —— `raise_without_activate` + `SetFocus` + 真实输入，界面确实切走（判据=「观察」按钮消失 + 5 秒复查），且 `foreground_stayed_with_user=True`（用户前台没被抢）。当时闭环把它设为首选（`click_client_nosteal` / `press_key_at`）；**2026-09-22 用户口径把点火流程改成"启动到前台 → 三下 → 切回后台"，这套回落链已整体删除**。**判据换过两次才对**：tick 不适用（观察者局开始暂停）、坐标必须先加 ROI 偏移。代价：SetFocus 期间键盘进游戏。详见 `自动化范式.md` §4.9 |
| **B46** | 真·隔离桌面（游戏跑在另一个桌面上、自带前台 ⇒ 用户桌面完全不受影响）**纯成熟库走不通**：`CreateDesktop` / `OpenDesktop` / `SetThreadDesktop` **pywin32 未暴露**（逐模块内省确认：`win32api` / `win32gui` / `win32ts` / `win32event` 都没有），而 `win32process.STARTUPINFO.lpDesktop` 可写、桌面却造不出来 | **封存**，不写。要用就得回到 ctypes（与用户口径"完全使用 python"冲突）。若将来用户放宽口径，这条是"真后台"唯一的正路 | 用户改口径或引擎改变输入读取方式时 |

## 三·补 · 环境漂移类红项（2026-09-22 夜复现）
| # | 问题 | 我的推荐 | 何时需要 |
|---|---|---|---|
| **B48** | **`v3 crosscheck` 的 18 条 token 红不是解析器错，是"我们读的文件 ≠ 引擎读的文件"**（2026-09-22 实测定性）：`test_engine_crosscheck.py::test_token行号与引擎一致` 报 18 条 `我们给出 None`（`gui/military_formation_panel.gui:1834 token='FLEET_SUPPLY_RAID_CHANCE'` 等，另有 `panel_military.gui`）。**逐条复核结论**：① 日志版本就是 **1.14.3**、`probe_session()=False`，不是旧日志；② 该 token **确实存在**于 `game/localization/english/ships_l_english.yml`；③ 直接用 `pdx.lexer.tokenize()` 读**原版**文件，**第 1834 行就是** `Token(STRING, '"FLEET_SUPPLY_RAID_CHANCE"', 1834:14)` —— 行号与 token 都对得上。所以差异只能来自 `cross_check()` 里的 `resolve(rel)`：它走 `build_override_map()`，**把原版路径解析成"某个 mod 的同名覆盖文件"**（本机挂了 Workshop mod）。也就是说：引擎那份日志来自一次**没生效这些覆盖**的会话，而我们按当前 mod 集去读 ⇒ 必然假红。**这不是本轮改动引入的**（本轮只动 `gametimer.py` + 测试 + README，不碰 lexer / engine_log；判据链 `tokenize → token_at` 与本轮无关） | 两个候选，**推荐后者**：① 给 `cross_check()` 传 `overrides={}`（它自己文档里就写了"引擎在无 mod 状态下跑过时用得上"）——但那就等于放弃"按实际生效文件核对"，削弱外部真值；② **让日志与覆盖集一致**：跑一次"与采集日志那次 mod 集相同"的常规会话再核对（日志是外部真值，不该迁就当前 mod 集）。无论选哪条，**先在各条红上打一行"我们读的是哪个文件"**（`resolve()` 的返回值），否则下次又会把它当成解析器 bug 查一遍 | 下次想用 `v3 crosscheck` 当真值时（它现在是**唯一**的外部真值通道，红了就等于这条通道没有读数） |
| **B50** | **非抢占点击路线有硬前提：游戏窗口必须可见（on top），只是不必是前台**（2026-09-22 实测两次）：`raise_without_activate` + `SetFocus` 那条路能点，但**判据要靠像素**（找「观察」按钮 = 模板匹配底部 ROI），而 `_owns_pixels()` 会先检查"这块像素确实属于游戏窗口" —— 别的窗口压在上面时它**拒绝抓图**（宁可超时报错，也不拿被遮挡的像素下结论，这是对的）。实测报错原文：「300 秒内没在底部找到「观察」按钮（最后抓图错误：现在压在上面的是别的窗口）」。**另一个实测发现**：加载结束的瞬间游戏窗口会**自己把前台抢走**（`前台仍然是你的窗口吗：False`），所以"加载阶段不碰窗口"能保证加载中不打扰，但**保证不了加载完前台还在用户手里** | 两条都要：① **文档补前提**（`自动化范式.md` §4.9 写明"可见但不必前台"），别再让下一个人以为它在完全被遮挡时也能跑；② 若要"完全被遮挡也能跑"，只能放弃像素判据、改用**引擎侧真值**（`ZZPROBE` 探针往 debug.log 写状态）—— 那要求先解 B47（探针 mod 挂载），所以**推荐先按①**：跑之前把游戏窗口露出来（`_show_no_activate` 只显示不激活，实测安全），跑完还原 | 下次做 G-EXIT-3 对照表时（每次都要进局） |
| **B49** | **游戏目录里的 2 个非游戏文件会自己回来**（B44 复发，2026-09-22 实测）：`gfx\portraits\accessory_variations\*.dmp` 与 `gfx\cursors\.XnViewSort` 再次出现（= 又一次有看图工具浏览过那两个目录），`game` 全树 27,723 → **27,725**，`v3 verify` 5 条 claims + `test_install_hygiene` 等 15 条用例同时红。**已按 B44 口径再次移出**到 `%TEMP%\v3_quarantine_20260921\`，复查 `v3 verify` **234/234 exit 0** | 保持现状（移出即可），但**把"门禁一红先看判据行"写进日常**：这 15 条红与当次改动毫无关系，只是那 2 个文件回来了。若将来频繁复发，再考虑让 `test_install_hygiene` 在失败信息里直接给出**移动命令**（现在已经给了处置说明，够用） | 每次"一片红但改动很小"时 |

## 三·补② · 完成度判据与意图层通道（2026-09-22 核）
| # | 问题 | 我的推荐 | 何时需要 |
|---|---|---|---|
| **B51** | **「较好完成度」目前没有可判定的定义**：`01-大方向.md` §4 只给 G1–G4 四道门，它判的是"能不能操纵 / 有没有因果 / 读不读得懂 / 退不退化"，**不判"mod 到什么程度算够好"** —— 于是"完成度"只能靠感觉说，而 `01a` 的 22 国示例表本该是那把尺子，现在没人拿它量过 | 把完成度**写成可数判据**（缺一项即不算好），建议五条：① **行为层差分**——A/B 两组在**可观测行为**（法律转换 / 改革时机 / 结盟偏向 / 宣战克制）上分开，只在策略卡名字上分开**不算**（阶段 3 已把这条写成硬口径）；② **档案库能填示例表**——`01a` 22 国示例表里的处境能被现有 schema 覆盖，覆盖不了的是抽象缺口；③ **抽象抗特例**——加一批档案**只加数据行**（G-EXIT-5.5），出现"必须写特例"即抽象不对；④ **三项工程底线同时成立**——五道闸门绿、预算有实测读数、装/卸/重装回原版基线（P12）；⑤ **维护可活**——官方更新后一次演练全绿。**这五条同时成立才叫"好"**；其中 ① 是唯一的实质门槛，其余四条是"没退化" | 下一轮定阶段 5 怎么收口时（现在就该定，否则每批档案都不知道朝哪收） |
| **B52** | ⚠️ **已写进档案 `why` 的一句机制说明是错的，必须改**（2026-09-22 实测复核）：`mod/data/ru_defeat.toml` 与 `tr_defeat.toml` 的 `[cards].why` 写着"开一个 JE = 同时喂给 **18 张**原版牌，不占任何槽位"。**实测**：`game/common/ai_strategies/*.txt` 里 `has_journal_entry` 共 **212 处**（`00_default_strategy` 177 / `02_diplomatic` 20 / `03_political` 10 / `01_admin` 5），读到的 JE 名去重 **60 个，全是原版自己的 JE**；`common` 全树（ai_strategies 之外）另有 **799 处**，逐条看也都是**硬编码的具名 JE**（`00_ai_triggers.txt` 的 38 处读的是 `je_alaska` / `je_opium_wars` / `je_tenpo_crisis` 这一串具名项）。**没有"读任意 JE"的通用入口** ⇒ 我们的 `je_sitai_*` **一张原版牌也喂不到**。JE 的真实作用有两处、都成立：**玩家可见性**（G3 三行解释的载体）与**世界状态判据**（其它原版脚本可用 `has_journal_entry` 读它）。**意图位移的真实通道是静态修正**（`progressive_agenda` 的权重读 `ig_*_is_powerful`，`03_political_strategies.txt:531/540`）—— 这条通道阶段 3 的 B-3 已实测动过（牌从 `reactionary_agenda` 落到 `conservative_agenda`） | 按 P10（每个数字/依据要真）**改两份档案的 `why`**，把"18 张牌"换成实测口径（JE = 可见性 + 世界状态判据，**不是** AI 输入通道；AI 读的是 IG 力量与修正字段）。同时把这条事实写进 `01a` 的引擎实测节。**注意这不动产物**（只改注释性 `why`），但仍要跑 `v3 modgen --check` 与两道闸门确认逐字节一致 | 下一轮改数据源时顺手做（它不影响产物，但影响"我们到底懂不懂引擎"这个判断） |

## 三·补③ · 阶段 3 重做：意图层的**真实通道**已查明（2026-09-22 第二次调研，全部带文件行号）

> 结论先行：阶段 3 的负结果（"牌换了名、法一条没改"）**不是设计错，是挂载点错**。
> 原版把"AI 动手改法"卡在**四处**，我们的档案一处都没碰到。以下每条都有 `文件:行号`。

| # | 机制（原文位置） | 它意味着什么 |
|---|---|---|
| **B53** | **`change_law_chance` 才是"动不动手"的闸门**：`03_political_strategies.txt:31` 保守牌 **2.5**、`:198` 反动牌 **3.5**、`:404` 进步牌 **10**。三张牌的 `ai_enact_weight_modifier` 只是"投票倾向" | 阶段 3 的 B-3 把牌从**反动**换到**保守** ⇒ **动手概率反而降了 1 点**。要真的动，落点必须是**进步牌**（10）或更强 |
| **B54** | **`max_progressiveness` 是覆盖型硬上限**：保守牌 50（`:19`）、反动牌 25（`:186`）、进步牌 100（`:392`）。而 `law_commercialized_agriculture` 的 `progressiveness = 50`（`01_land_reform.txt:347`） | 落在"保守"上**恰好顶在临界值** ⇒ 连"能不能碰这条法"都在边缘 |
| **B55** | **`law_tenant_farmers` 的 +1000 有一条近乎无条件的支**（`01_land_reform.txt:216-231`）：`has_law_or_variant = law_serfdom` + `NOT has_strategy = reactionary` + (`NOT has_strategy = conservative` **或** 某运动赞成且支持 ≥10%) | 换成**任何既非反动也非保守的牌**，这条 +1000 **无条件成立**。而 `movement_land_reform` 对商业农业是 `neutral`（`03_ig_ideologies_movement.txt:721`）⇒ 走运动那支对它是**假** |
| **B56** | **`has_enactment_je_or_law_commitment` 与 `ai_has_enact_weight_modifier_journal_entries` 是硬编码白名单**（`00_ai_triggers.txt:89-99` / `:144-168`），但**各有一条通用支**：`:97 has_any_law_commitment = yes`、`:146 has_law_commitment = scope:law.type` | **开一个自己的 JE 不等于喂给原版牌**（B52 已证）。真正通用的是 **law commitment**：一次命中"9 张政治牌 +25 改法概率"与"131 处法律 +750 权重" |
| **B57** | **`law commitment` 唯一创建路径 = `create_treaty` + `article = law_commitment`**（模板 `history/treaties/00_historical_treaties.txt:819-832`；JE 内调用实例 `journal_entries/03_afghanistan.txt:546`）。`add_law_commitment` **不存在** | 而且 **AI 自愿签几乎不可能**（接受度基础 −100、已有同类 −300、对方持落后法 −1000，`treaty_articles/20_law_commitment.txt:272-387`）⇒ 只能**脚本直置** `is_draft = no` |
| **B58** | **`set_strategy` 是脚本效果**（`effect_localization/00_country_effects_loc.txt:40`），原版在 JE 里就这么用（`journal_entries/05_grunderzeit.txt:118`、`events/peoples_springtime.txt:780/980/1097`） | 它**绕开** `CHANGE_STRATEGY_THRESHOLD = 100` + 每周 20% 的累积掷骰（`defines/00_ai.txt:38-39`，期望 ≈9.6 年一次）—— 这是"窗口一开就换路线"的合法手段（F5 的 B 级：整条路线要换才递牌，且**递的是原版自己的牌**，不占我们自己的槽） |
| **B59** | **`is_powerful` 的判据是 clout 份额**（`defines/00_defines.txt:189-190`：`POWERFUL_IG_THRESHOLD = 0.20` / `CUTOFF = 0.18`）；`interest_group_ig_*_pol_str_mult` 真实存在、原版最大档 **+2.0**（`static_modifiers/content_304_modifiers.txt:228`） | 我们的 `[reform_inputs]`（+0.5）**方向对**，但**固定倍率不保证跨界**（分母会摊薄）——必须实测，不许盲调 |
| **B60** | **JE 里硬改法只能在 `on_complete`**：JE 目录 13 处 `activate_law` **13/13 全在 `on_complete`**（如 `06_economic_regeneration.txt:751`）；`start_enactment` 在 JE 目录 **0 处** | "JE 一开就换法"在 `on_activation` 里**做不到**；`on_complete` 是原版合法写法，但它绕开"AI 自己推导"，只配当兜底/对照 |

**对阶段 3 重做的直接含义（判据已预注册）**：
1. **世界层不变**（战败记忆变量 + 真实贵族/合法性/财政变化 + 窗口 JE）；
2. **意图层换成"窗口打开 ⇒ `set_strategy ai_strategy_progressive_agenda`"**（B53/B55/B58），
   并让**窗口关闭时递回** `ai_strategy_reactionary_agenda`（"窗口是开着的一段"）；
3. **可选加一道 law commitment**（B56/B57）验证 +750 的独立作用；
4. 验收口径不变：**法律真的换**（`law_serfdom` → `law_tenant_farmers` 或
   `law_commercialized_agriculture`）才算 H2 成立；只换牌名**仍然算不成立**。

| # | 问题 | 我的推荐 | 何时需要 |
|---|---|---|---|
| **B61** | **重跑阶段 3 的会话预算**：每局 ≈3 分钟加载 + 观测若干月；一条 A/B 至少两局 | 先跑**一局**验证"递进步牌 ⇒ 真的换法"（这是 B53-B58 整条链的**唯一未实测环节**）；成了再做 A/B 差分 | 立即（阶段 3 重做的第一步） |

## 三·补④ · 阶段 4 唯一未达成的出口判据（G-EXIT-3 对照表）卡在哪（2026-09-23 实测）

| # | 问题 | 我的推荐 | 何时需要 |
|---|---|---|---|
| **B62** | **覆写 `gui/error_deer.gui` 里已有按钮的 `onclick` 不生效**：点下去走的是**原版行为**（打开错误列表 —— 有画面证据：点前写着「84 个错误」、点后错误列表被打开），而这一版覆写**连一条 error 都没有**（上一版少写外层引号时引擎明确报 `gui/error_deer.gui:42 - '(' is not a valid widget/type/property`）⇒ **文件被解析了，但引擎用的仍是原版那一份**。已排除：坐标（模板匹配 1.000）、前台（真实输入、画面确实变了）、命令拼写（外层引号已补）。**未排除**：本地 mod 的挂载顺序 / 有没有 gui 缓存（用户目录下**没有** gui 缓存目录，只有 `shadercache`）/ 或者 `error_deer` 这个面本身不吃覆写 | 三条候选按代价排：① 先看 **B39 的机制**（本地 mod + Workshop 混合时挂载异常）是否同源；② 试 `log_ticktask_performance` → `profiling.log`（exe 里 1 命中，**持续写、不需要 dump**，若能被一次性开关打开就绕开 GUI）；③ 最后才回头啃覆写 | 跑 G-EXIT-3 对照表之前 |
| **B64** | **触发逐任务计时的**九条路全部判死**（2026-09-23 一轮内逐条实测）**：① 自建顶层 widget ⇒ 不实例化；② 往内层 `flowcontainer` 开头插 ⇒ 解析通过但 0 像素；③ 改已有按钮的 `onclick` ⇒ 命令没进控制台；④ 颜色质心定位 ⇒ 按钮**没有可测颜色**；⑤ 模板匹配 ⇒ 同上；⑥ 六个候选坐标逐个试 ⇒ 全不生效；⑦ 加 `using = default_button` ⇒ 按钮**画出来了**（截图里看得见 `TickTimer` 文字）但点击仍不触发；⑧ 命令行 `-run_console_action=` ⇒ **进程当场退出**；⑨ 命令行 `-run_console_action_main=`（"runs once the game has been initialized"）⇒ **同样当场退出**（180 秒没等到窗口）。**同时拿到三条硬结论**：(a) gui 覆写**确实会被加载** —— 往 `error_deer.gui` 塞一个不存在的控件类型，`error.log` 立刻报 `gui/error_deer.gui:34 - 'sitai_deliberate_syntax_error' is not a valid widget/type/property`；(b) **exe 的命令行开关表里没有"让逐任务计时常驻"的开关**（有 `-debug`/`-debug_mode`/`-script_profiling`/`-scripted_tests`/`-ui_validation`/`-force_full_modifier_recalc` 等，**没有** `-log_ticktask*`）；(c) `run_console_action` 一族只接受 `console_history.txt` 里那种**裸命令名**（`log_ticktask_performance` 或 `dump_ticktask_timings`），带 `&`/`;`/`\|` 的复合写法**会崩在启动早期** | 剩下**两条**：(1) 让人**手打一次**、只回答一个是非题 —— "在游戏控制台里敲 `dump_ticktask_timings`，`Documents\…\ticktask_timings.csv` 会不会出现？" 这一条数据能定性"命令本身是不是坏的"，比继续在工具侧猜便宜得多；(2) 接受"GUI 触发不可靠"，改成**半自动**：脚本跑一局 → 提示"请在游戏里按一下那个按钮"（在 `error_deer` 调试覆盖层里，肉眼可见，写着 `TickTimer`）→ 小时级等待 → 收 `profiling.log`。**牺牲无人值守，保住读数** | 决定 G-EXIT-3 是否继续投人时 |
| **B65** | **大厅/选国那张地图自己会平移 —— 动的画面不能用"紧邻两帧像素差"当判据**（2026-09-23 实测）：在主菜单扫控制台候选键时，反引号读出**上半屏像素差 28.365**（远超阈值 3.0），看起来像"面板开了"；把两张截图裁出来一比对，**只是地图镜头向东偏了几百像素**（`丹麦`/`瑞典王国`/`大不列颠` 的相对位置变了），根本没有控制台。⇒ 结论有两条：① 像素差判据只对**静止画面**成立，扫描必须在**暂停的局内**做；② `tools/probe/console_probe.py` 因此把大厅扫描整段删掉（只留局内），并把停住画面写成流程的一部分 | 已落到代码里（探针只在局内扫）；若将来还要在大厅做像素级判定，先找**不动的**参照物（例如固定 HUD 元素），不要用全屏差 | 任何"用画面变化当判据"的探针 |
| **B66** | **性能仪表通了：走游戏内控制台，`dump_ticktask_timings` 已拿到实物**（2026-09-23 实测，把 B64 从"九条全判死"翻过来）。三条分开验证的结论：① **反引号 `` ` `` 开控制台**（`shift+\``/`/`/`f12` 都无效，上半屏像素差 0.000；判据是输入框 ROI 均值：开着 ≈31–48、关着 ≈190）；② **合成的扫描码键盘进得去**（截图里逐字读得到命令名）；③ **要按两次回车才提交** —— 按一次标准差 44.9→44.0（字一个没少），连按两次 43.0→**7.5**（清空）且输出区亮像素 322→**1732**。**第一次回车被自动补全吃掉**（exe 明文 `SETTING_CONSOLE_AUTOCOMPLETE_MODE` 一族）—— 这正是 9/21 那次"命令进了 `console_history.txt` 但文件没出现"的真原因 | 已收进 `pdx.game_auto`：`console_open` / `open_console` / `submit_console_command`（+ `CONSOLE_EDIT_ROI` / `CONSOLE_OUTPUT_ROI` 两条画面判据），`tools/probe/perf_compare.py` 与 `console_probe.py` 都走它 | 已落地（G-EXIT-3 的对照表因此可跑） |
| **B67** | **`log_ticktask_performance` → `profiling.log` 仍然否定，原因查明**：控制台**明确回话** `Tick task logging enabled, output: profiling.log`（截图读到），但文件始终不出现。`platform_specific_game_data/log_settings_live.json` 的 26 个 logger category 里**没有 `profiling`（也没有 `ticktask`）** ⇒ 这条日志**没有 sink**。按实测给它补了三个候选类别（`profiling`/`ticktask`/`gamemetrics`，各配 `file` sink，先备份、收尾还原），文件**仍不出现** | **暂判不可用**，不影响 G-EXIT-3：`dump_ticktask_timings` 本来就更合适（**有界窗口**：clear → 跑 N 月 → dump；且**有 frame 列**）。要再试的话，下一个变量是"类别名"：从 exe 里找写这条日志的代码路径，或试 `tick_tasks`/`gametimer` 这类名字 | 只有"必须持续写日志"的场合（本项目的预算不需要） |
| **B68** | **阶段 4 侦察里那个悬案的假设更新**：12 份 `gametimer_*.tsv`（2026-09-13）是**旧构建**产出的，此后一份都没有；当时列了"构建变了 / 启动方式变了"两种可能，都没排除。B67 给出第三种、也是目前最像的：**那一类没有 sink**（`log_settings_live.json` 里既没有 `gametimer` 也没有 `profiling`）。**仍未定论**：旧构建的日志表没留存 | 不必再查 —— `gametimer_*.tsv` 只到 `Day` 粒度（没有帧列），本来就不能判"单帧 ≤0.5ms"；帧级真值已经由 `ticktask_timings.csv` 提供 | 只作为记录（免得后人重开这条线） |

## 三·补⑤ · 阶段 5 批次 1（两份非战败档案）暴露出来的东西

| # | 问题 | 我的推荐 | 何时需要 |
|---|---|---|---|
| **B69** | **"激进派占比"这条判据在本 schema 里表达不出来**（写 `au_revolution` 时发现的）：① 原版 1.14.3 没有能在**国家作用域平铺**读激进派占比的触发器 —— `radicals_percentage` 原版 0 处、`radical_fraction` 是**块形式**（`00_strike_triggers.txt:53-60`）、`political_movement_radicalism` 是**运动作用域**（`05_austria_journal_entries.txt:42-45`）、唯一平铺的 `any_insurrection_ongoing`（`00_scripted_triggers.txt:1117`）要求"已出现叛乱运动"（更晚更严，**未实测**）；② 就算有，`modgen.Clause.render` 只支持**平铺一行**，块形式写不进数据源 | 两条路：**(a) 让步**（推荐）—— 激进派状态由 `[pressure]` 的 `country_radicals_from_legitimacy_mult` 承担（引擎自己的公式读它），门只开在合法性上；**(b) 改抽象** —— 给 `Clause` 加"块形式"表达（那是 schema 改动，照阶段 5 三行"档案加不进去 ⇒ 改抽象，不是加特例"办） | 想要"革命期的门"真正读革命强度时 |
| **B70** | **`[panel]`（P11 三行）曾经只有 2/4 份档案有**：`ru_defeat` 与 `cn_intervention` 有，`tr_defeat` 与 `au_revolution` 没有（写它们时照的是"骨架"口径）。三行是阶段 6 的出口判据（试玩者能复述"目标 + 主因"）的载体 | ✅ **已补全**（2026-09-23）：两份档案各补 `[panel]` 三行 + `why`，文案**从各自已有的压力字段与判据派生**；`v3 modgen --write` 后四份档案的 `_reason` 都是 5 段（原说明 2 段 + 三行）。**还没做**：把"每份档案都必须有 `[panel]`"写成闸门/用例（现在靠纪律与 `v3 modgen` 的可选表语义） | 想让它变成硬判据时 |
| **B71** | **生成器曾经把"战败"写死在注释里**（`modgen.py:925-928` / `:992`），于是非战败档案的产物注释是错的（"只生成不手写"之下人改不了产物）。**已修**：注释改成由档案自己的 `title` 说话，不再替档案断言处境 | 已落地（属 G-EXIT-5.5 允许的"抽象改进"，记在 `exec/阶段5-批次1-结果.md`）。**教训**：生成物的文案里不许出现只对某一类档案成立的词 | 已修 |
| **B72** | **既有档案里有一句事实错误**：`ru_defeat` / `tr_defeat` 都写「`country_loan_interest_rate_add` 原版**只出现过一次**」。实测（1.14.3，`common/`）**12 处**：全局基准 `00_code_static_modifiers.txt:12 = 0.2`、法律侧 `00_amendments_enactment_04.txt:644/1138/1174/1222/1266 = 0.02/0.02/0.03/0.01/0.05`、科技侧 `30_society.txt:209/599/1226/1473/1647 = -0.02`、字段定义 `00_modifier_types.txt:662`。「国家静态修正那边没有档位阶梯」仍成立 | **已修**（两份档案的 why 都改成实测事实）。**教训**：这类"原版只出现过一次"的断言必须由工具给数 —— `v3 citations` 只查"引用指得到"，查不出"计数断言"。要真防住，得再有一条"计数断言"检查（记在这里，未做） | 已修 |

## 三·补⑥ · 阶段 6 的界面事实（2026-09-23 实测，决定了"三行往哪儿放"）

| # | 事实 | 依据 | 影响 |
|---|---|---|---|
| **B73** | **JE 面板显示的是 `<JE 键>_reason`** | `game/gui/journal_entry.gui:742` 的 `text = "[JournalEntry.GetReason]"` | 三行解释要上屏，就得接进 `_reason` —— **已落地**：`v3 modgen` 把 `[panel]` 三行按字面量 `\n\n` 接在 `_reason` 后面，并且**缺 `_reason` 就报错**（见 `exec/阶段6-可见性与难度.md` §一/§二） |
| **B74** | **`<JE 键>_goal` 那一槽不保险**：引擎会报 `journal_entry_type.cpp:476 Journal entry has redundant loc for <JE 键>_goal`（实测，`game.*.log`）。原版确实大量用 `je_*_goal`，但那句 `redundant` 说明取决于 JE 形态 | 引擎日志原话 + 原版样例对照 | 口径定为「**"上屏"以 `_reason` 为准**，`_goal` 只当加分项」。想彻底查清"什么时候 redundant"要做一次带 `_goal` 的 A/B 截图对比（未做，不值得为它单开一局） |
| **B75** | **生成物里"真换行"会把 `.yml` 写坏**：本地化值的换行是**字面量** `\n\n`（两个字符，committed 的 yml 每条 key 一行、文件 9 行）。第一版用真换行拼接 ⇒ yml 变 12 行、`_reason` 的值被拆成 4 行而**后三行没有 key**，反解时静默丢掉 | `modgen.write()` 原样写文本；committed yml 可逐行复核 | **已修**（拼接改用字面量），并写了用例钉住"每条 loc 一行"这条不变量。教训与 `_why` 遮蔽那条并排记着：**在生成器里拼文本，换行与作用域都要当成"会有坑"的地方** |
| **B76** | 难度三档 `game_rules` **已落地**（2026-09-23）：机制用原版有先例的那一种（`flag` + 脚本侧 `has_game_rule`，原版 7 处在读），`apply_modifier` 因"1.14.3 原版 0 处使用"而**不用**；三档差异接在冲击效果上（`is_ai = no` + `has_game_rule`）；契约在位的检查放在**真实数据源的用例**里，不放编译期 | ✅ 已做 + **实机加载期实证**（只装我们的 mod 跑一局：`Mounted Data` 有我们、`common/game_rules` 被枚举且无解析错、全日志搜 `has_game_rule`/`sitai_difficulty` **零报错**）。**未做**：规则界面里那三档没看过（观察者局进不到开局规则界面） | UI 确认留待一次"以国家身份开新局"的会话 |
| **B80** | **探针的行为层读数带俄国口径**（2026-09-23 跑 `au_revolution` 的 A/B 时暴露）：`stage3_rerun.analyze()` 的"第一次非农奴制"启发式拿观测到的法与 `law_serfdom` 比 —— 奥地利在候选表里读到的是 `law_autocracy`，于是"法律真的换了"在**没换**的情况下报 True。真正的行为层读数是 `ENACT` 那一行（26/26 全是 `none`），它没被骗 | ✅ **已全部做完**（2026-09-23）：① 那个会骗人的派生布尔从分析器里删掉；② 数据源新增 `[probe]` 表（**每份档案显式声明** `reform_law`，六份都写了：ru/cn/pe = `law_serfdom`、au = `law_manorialism`、tr/eg **留空并写明为什么**）；③ 套件按档案判 —— **留空就不产出那条判据**（写死 `law_serfdom` 时对没有农奴制的国家它立刻为真，实测踩到）；④ 探针武装时写 `ZZPROBE AB;TARGET;<档案 id>`，分析器靠它认主语（日志里的国名是**本地化的**、不是 tag）；⑤ 月度 `LAWS` 补上庄园制与 latifundias 那一组（少列一条某国的土地法就读不到）；⑥ 新增 4 条用例（含"没声明就不判"和"TARGET 行随档案变"）。**教训**：任何"跨档案通用"的判据都要问一句"它对别的档案是不是立刻为真" | 已闭环 |
| **B81** | **生成器的 effects 只渲染 `<字段> = <数字>`，布尔字段表达不出来**（写 `eg_debt` 时暴露）：`country_force_privatization_bool`（原版"列强财政接管"最像的字段，`00_code_static_modifiers.txt:243` 与 `00_ip4_04_modifiers.txt:139` 都有先例）只能写成 `= yes`，而 `Param` 只带 `amount`/`arg`（`arg` 是字符串标识符，渲染路径也不同） | 两条路：**(a)** 给 `Param` 加 `flag: bool` 或允许 `amount = yes/no` 的字面量渲染；**(b)** 数据源里换一个数值字段表达同一件事（本批选的就是 (b)，`why` 里如实注明"布尔表达不出来"）。要真支持布尔，得同时想清楚闸门 ③ 的价格表与闸门 ④ 的事实表怎么记它 | 想用布尔/枚举字段表达处境时 |
| **B82** | **`modguard` 闸门 ② 的合成夹具（`test_modguard._fake_game`）每加一份档案就要补一遍**：它"按需列出原版有什么"，新档案用到的新 tag / 图标 / 分组 / 触发器等都要手工补进去（阶段 4 加 tr、阶段 5 加 au/cn、加 eg/pe 各踩过一次）。它守的其实只是"闸门代码没坏"，而真游戏上 `v3 modguard` 是全绿的 | 让它**从真实游戏目录自动取所需词条**（或对真实数据源跑、无游戏时跳过）。**未做** —— 当前的处理是"红一次、补一次夹具"，代价可接受但会重复 | 加档案变频繁时 |

| **B83** | **doc 12 那批「本机 mod 快照」分布数曾整批算错，根因不是「用户改了订阅」**（2026-09-23 暴露，同日**已闭环**）：表面症状是 `mod.total` 23→24、`mod.files` 4,777→4,828，当时判成「用户新订阅了一份 Workshop mod」。**真实原因是我们自己** —— 跑 A/B 时部署进用户 mod 目录的那份**真 mod** 被 `pdx.mods.discover_mods()` 当成了订阅项（它当时只排除 `zz_probe_` 前缀）；而档案集合从 6 变 8 之后，`stage3_rerun.restore()` 拿**新目录名**去找**旧目录** ⇒ 那份副本永远删不掉。⚠️ **这条坑 `exec/收口清单.md` 里早就记过一次**（那次是 `sitai_ru_defeat`、差 8 个文件），说明「红一次、手工删一次」这种修法**不足以防止复发** | ✅ **已按结构性修法闭环**：① `discover_mods()` 改按 `.metadata/metadata.json` 的 `id` 前缀认「这是我们自己的 mod」（`mods.OWN_MOD_ID_PREFIX`，**不再看目录名**）；② `stage3_rerun._clean_ours()` 在部署前与还原时清掉**所有**属于本仓库的本地目录，一个都没删到时会出声（P13）；③ 命名空间常量上收到 `config.NAMESPACE_PREFIX`（生成器与扫描器同源，P9）。**重测结果（23 个 Workshop mod）**：文件 4,796、去重路径 4,769、`localization` 条目 1,475、§1 使用率表逐行不变、§9 第 4 条的 10,379 + 807 逐条对上。**顺带修掉三处口径错**：doc 12 §9 第 4 条把「全部目录的条目数」写成了「`common/` 下」（`common/` 只是**文件数**那一列的口径）；§7 的版本分布 8/10 与 §9 的 9/9 互相矛盾（实测 9/9）；§4 的「文件数」含各自 `metadata.json` 而从未写明。**另有一处真更新**：用户订阅的 `2880069248`（牛奶汉化）确实更新过（+19 个本地化条目 / +19 文件），所以「4,828 → 23 个 mod 的真值」**不能只做减法** | 已闭环 |

| **B84** | **引擎报的 `scripted_effects` 文件行号与生成结果对不上**（2026-09-23，跑 `bv_alignment` 的 A/B 时发现）：**同一局、同一份探针、同一时刻**，`common/on_actions/zz_probe_ab_on_actions.txt` 的行号与重新生成的结果**逐行对上**（引擎报 25 / 39 / 50 / 60 / 182 / 189 / 207 …，生成结果完全一致），而 `common/scripted_effects/zz_probe_ab_effects.txt` **对不上**（引擎报 `SELFARM;13`→49、`SELFARM;37`→58、`RUN;B`→81；重新生成是 55 / 66 / 86） | **未查**。本仓库多处拿"引擎报的行号"当证据（如 `zz_probe_mod_calls.txt:27`），所以这条要么查清偏移规则、要么在口径里写明"只有 `on_actions` 的行号可信"。**不挡任何结论**：`阶段5-实机读数补记.md` 的读数一条都不依赖行号 | 下次需要拿 effects 行号当证据时 |
| **B85** | **分析器读轮转日志的顺序错了，读数因此是一段「不连续的切片」**（2026-09-23 跑 `sp_empire_remnant` 时发现，**同日已修**）：`debug.log` 到 512 KB 就轮转成 `debug.1.log` …（一局 66 个月产生 4 份）。`analyze()` 原本用 `sorted(base.glob("debug*.log"))`，而**字典序是** `[debug.1, debug.2, debug.3, debug.4, debug.log]` —— 当前那份排最后、次新的排最前。再叠上"按 `RUN` 行切片"（原意是排除上一局留下的尾巴），拼接文本里"最后一次武装"就落在最旧那份的中间 ⇒ **`debug.3`/`debug.2`/`debug.1` 整份被丢掉、最新的 `debug.log` 被接在后面**。实测：**盘上 1,401 行自报，分析器只报了 402 行**，并且报出一个**假的**「窗口 `inactive → active`」 | ✅ **已修两处**：① `debug_logs()` 按**时间**排序（编号大的在前，`debug.log` 最后）；② `--fresh-logs` 时**不再按 `RUN` 剪**（观察者局没有 `RUN;A`，那把剪刀会剪掉这一局自己的前 37 个月）；③ 读数同时报**盘上自报行数**与**算进读数的行数**（被剪掉多少一眼可见），并新增 4 条用例钉住排序与剪切语义。**已受影响的读数**：`bv_alignment` 与 `eg_debt` 两局（逐格标了稳/不稳，见 `exec/阶段5-实机读数补记.md`）；`sp_empire_remnant` 已按新口径用 `--analyze-only --whole-log` 重算（不重开游戏）。⚠️ **同一轮里还犯过一次同源的错**：上一轮把「`TARGET` 自臂行没落」当成结论，其实只是 grep 了 **`debug.log` 那一份**（重跑实测：`TARGET` 在 `debug.1.log` 里 1 条、`debug.log` 里 0 条）—— 凡"日志里有没有 X"，口径必须是**整个日志集合**，只看当前那份得到的"没有"**从来不是证据** | 已修 |
| **B86** | **探针的「进步牌挂上过」是一条跨档案通用判据，而巴西的初始政治牌里本来就有它**（2026-09-23 做「失去市场」侦察时发现，**✅ 同日已修：候选 A 落地**）：`common/history/ai/00_strategy.txt:42-45` 的 `c:BRZ ?= { … set_strategy = ai_strategy_progressive_agenda }` ⇒ 在巴西身上这条判据**开局即为真**，G-EXIT-5.3 会报一个假的「行为层动了」。这正是 `收口清单.md` 那句「任何跨档案通用的判据都要问：它对别的档案是不是立刻为真」（B80 同族） | **A（倾向）已做**：每份档案的 `[probe]` 显式声明 `reform_card`（与 `reform_law` 完全同构，可留空 = 不判），套件与分析器都按声明判（`modgen` 读数据源 + `probe_stage3._card_reading`）；8 份档案声明 `ai_strategy_progressive_agenda`，`brz_market_loss` 留空并写明理由（开局就挂着它）。未采纳 B：相对判据要引入「开局牌从哪来」的新依赖。详见 `exec/阶段5-失去市场-侦察.md` §三 | 已修（2026-09-23） |
| **B87** | **探针的政治牌读数是一份手写的三张牌清单，而白名单里递得出去第四张**（2026-09-23 收口 B86 时顺带发现，**✅ 同日已修**）：`ab_probe.POLITICAL_STRATEGIES` 只列了 progressive / conservative / reactionary 三张，链尾**又没有兜底** ⇒ 国家挂着 `ai_strategy_egalitarian_agenda`（**就在 `modgen.SET_STRATEGY_WHITELIST` 里**）时**这个月一行都不记**；而「没记」与「没有牌」在这套日志里长得一模一样，分析器按 `reform_card` 去问就得到**必然为假**的结论。原版政治槽实际有 **10 张**牌，漏掉的另外 6 张（`great_reforms`、`maintain_mandate_of_heaven`、`meiji_restoration`、`nationalist_agenda`、`tanzimat_reforms`…）同样读不到 | 候选集**改成从原版现读**（`strategy_candidates()` → `common/ai_strategies/` 里 `type = political` 的全部牌）+ 补 `ai_strategy_default` 与 `none` 两条兜底；分析器把「一行都没有」与「牌是 none」**分开报**（前者是读数缺失，不是「牌没挂上」）；`v3 ab-probe` 新增 `--archive`（默认取文件名第一份，而它随新档案入库而变 ⇒ 复现某次实验必须写出档案名）；探针 metadata 的 `supported_game_version` 改为与真 mod 同源（原写死 1.14.3，mod 升 1.14.4 后启动器会对探针弹版本警告）。测试两侧都加：候选集必须覆盖白名单里的每一张牌、白名单必须都在原版政治槽里 | 已修（2026-09-23） |
| **B88** | **档案的「改革侧输入」喂的是牌的权重输入，而实测证明那一侧不是瓶颈**（2026-09-23，从 `tr_defeat` 那一局的逐月读数归因出来，见 `exec/阶段5-实机读数补记.md` §三·补.2）：奥斯曼那一局**牌没换**（`tanzimat_reforms` 在朝 63 个月，`change_law_chance = 100`）、**目标法正确**、**AI 自己推了那条土地法 33 个月**（`is_enacting_law` 连续为真）、改革派一直在朝 —— 而**合法性从冲击那一月起落进最低档（≤55）并再没回来**，法 33 个月没推成。⇒ 阻塞在**立法的「过不过」那一侧**，而 `[pressure]/reform_inputs` 动的是 IG 政治力量（= 牌的**权重输入**） | ① **正式版设计**：要让改革派的实力强到能组出**高合法性**政府（压 landowners 的**制度底座**），而不是继续给改革派加成；② **读数手段**：`CLOUT` 是单调夹逼读数（`≥ b03…b25`），**量不出某一档之内的变化** —— 上表 landowners 与 intelligentsia 都是 b25，我们的 −30% 在读数里看不见。要验证压力是否生效，得换一个档内可分辨的读数（候选：`legitimacy` 的更多档位、或按 `is_enacting_law` 的成功/停滞比） | 写正式版设计时 |
| **B77** | ~~**`v3 citations` 进不了 CI**（当前 CI 的唯一实质缺口）~~ **✅ 已做（2026-09-23）**：精简快照新增 **`citation_support` 域**（形状与其余域一致：`引用写法 -> ["lines=N", "<行号>:<sha256 前 16 位>", …]`，151 个名字 / 1,065 条），由 `v3 snapshot create --compact` 写入；`v3 citations --offline` 只读快照核「每条引用都还在入库支撑域里、且没超出当时记的行数」，**CI 已加这一步（check job 现 13 步）**。口径与其它离线通道一致：**证明"与入库快照一致"，不证明"与现在的游戏一致"**。附带一个**在线上才有的**信号：有游戏时 `v3 citations` 会顺手核「被引那一行**还是不是那句话**」（指纹比对）—— 那正是「官方更新把我们的依据挪走了」的第一手信号，也是 `01-大方向.md` §5 版本演练要的东西 | ✅ **已完成**。刷新方式：改过引用之后跑 `v3 snapshot create --compact`（快照是入库的；本次刷新只多了这一个域，diff +1,369 行）。**新增 8 条用例**（域的形状 / 行数与指纹 / 离线不看在现场 / 在线核出漂移 / 真实数据源的域与现场一致 / 快照里确实带这个域） | 已闭环 |
| **B78** | **一次"从 A 到 B 之间整段替换"的手术切掉了两个函数**（2026-09-23 实测）：改 `stage3_rerun.quarantine_logs()` 时用"从它到 `def analyze(`"整段替换，而这段里还夹着 `_months_of` / `wait_months` —— 症状只在**实跑到那一步**才出现（`NameError: wait_months`），静态检查全绿（ruff/mypy/pytest 都不读探针脚本） | 已恢复。**纪律**：整段替换前先把那一段**打出来确认只含要换的东西**；探针脚本（`tools/probe/*`）没有用例守着，改它们要更小心（这一条同时是"探针要不要进测试覆盖面"的证据，未决） | 下次改探针脚本时 |
| **B79** | **崩在 `finally` 之前会把用户的 mod 配置留在改动状态**（2026-09-23 实测）：`stage3_rerun` 的顺序是 `deploy()`（改 `content_load.json`）→ `quarantine_logs()`，而后者抛 `PermissionError` ⇒ 崩在 try 之前，`finally` 的 `restore()` 根本没执行，用户那 23 条 Workshop 配置留在"只剩 1 条"的状态 | **已修两处**：① 顺序换成"**先挪日志、再改配置**"（挪日志只动临时目录，失败则什么都没改）；② 把"挪日志（跳过被占用的）"收进 `pdx.game_auto.quarantine_logs()`，两个探针共用（原来两份实现，只有 `perf_compare` 那份带守卫）。**通用教训**：任何"改用户环境"的脚本都要保证**第一次改动之前**不会抛 | 已修 |



## 四、示例表待补（不挡主线）
| # | 问题 | 我的推荐 | 何时需要 |
|---|---|---|---|
| B22 | 德意志中小邦 / 意大利诸邦 / 印度土邦 / 奥斯曼属国的档案（"倒向谁"） | 阶段 5 扩张时补一组；它们与主角国家逻辑不同 | 阶段 5 |
| B23 | 22 国示例的三槽倾向校准 | 阶段 1 清单出来后复核一遍 | 阶段 1 |

## 五、明确不做（记录在案，避免反复讨论）

| # | 事项 | 结论 |
|---|---|---|
| N1 | 兼容第三方 mod（含 Kuromi's AI / ARoAI 等） | **不做** |
| N2 | 整文件替换原版（R3） | **禁用** |
| N3 | 关原版子系统（R4） | **封存**（仅 H1 不成立且逐案批准） |
| N4 | 给 AI 数值作弊、学玩家打法、钻机制漏洞 | **不做** |
| N5 | 手写脚本（不用生成器） | **不做** |
| N6 | "让 AI 会做某个 C++ 操作"（建筑选址、军事调动、市场选择） | **做不到**，只承诺改它读到的值 |
