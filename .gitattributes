# ─────────────────────────────────────────────────────────────
# 行尾与文件类型规范
#
# 不加这个文件时，`core.autocrlf=true`（Windows 上的常见默认）会让
# 每次 git add 都弹「LF will be replaced by CRLF」警告，并且同一个文件
# 在不同机器上检出成不同的行尾 —— 对 Python 来说这不只是噪声：
# 它会让 `git diff` 出现整文件级别的假差异，也会让基于内容哈希的
# 校验（如黄金回归）在换机器后莫名其妙地失败。
# ─────────────────────────────────────────────────────────────

# 默认策略：由 git 自动判定文本，文本一律以 LF 入库
* text=auto eol=lf

# ── 明确的文本类型 ─────────────────────────────────────────
*.py      text eol=lf
*.md      text eol=lf
*.txt     text eol=lf
*.toml    text eol=lf
*.ini     text eol=lf
*.cfg     text eol=lf
*.json    text eol=lf
*.yml     text eol=lf
*.yaml    text eol=lf
*.csv     text eol=lf

# ── 二进制类型（禁止任何行尾转换）──────────────────────────
#
# 前瞻规则：当前仓库里**一个都没有**（168 个跟踪文件的扩展名只有
# .md/.py/.yml/.yaml/.toml 与无扩展名）。留着是为了将来真把素材或
# Excel 塞进来时不会被行尾转换破坏 —— 这类破坏一旦发生就不可逆。
*.prof    binary
*.pyisession binary
*.png     binary
*.jpg     binary
*.jpeg    binary
*.gif     binary
*.webp    binary
*.dds     binary
*.tga     binary
*.ico     binary
*.xlsx    binary
*.pptx    binary

# ── diff 策略 ──────────────────────────────────────────────
# research/official-docs/ 是逐字镜像，diff 只会是整文件噪声，不展开。
# 注意这里**没有** `tools/out/** -diff` —— 该目录在 .gitignore 里，
# git 不跟踪它，规则永远不会命中（曾经有这条，属空转）。
research/official-docs/**  -diff

# ── GitHub 语言统计 ────────────────────────────────────────
# research/official-docs/ 是游戏官方文档的逐字镜像，不算本仓库的代码贡献
research/official-docs/**    linguist-vendored
tools/tests/test_golden/**   linguist-generated
